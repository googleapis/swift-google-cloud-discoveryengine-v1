// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

#if AssistantService
  import Foundation
  @_spi(GoogleCloudInternal) import GoogleWKT

  /// Request for the
  /// [AssistantService.StreamAssist][google.cloud.discoveryengine.v1.AssistantService.StreamAssist]
  /// method.
  ///
  /// [google.cloud.discoveryengine.v1.AssistantService.StreamAssist]: <doc:AssistantServiceClient/streamAssist(request:options:)>
  public struct StreamAssistRequest: Codable, Equatable, GoogleWKT._AnyPackable,
    Sendable
  {
    /// Required. The resource name of the
    /// [Assistant][google.cloud.discoveryengine.v1.Assistant]. Format:
    /// `projects/{project}/locations/{location}/collections/{collection}/engines/{engine}/assistants/{assistant}`
    ///
    /// [google.cloud.discoveryengine.v1.Assistant]: <doc:Assistant>
    public var name: Swift.String = Swift.String()

    /// Optional. Current user query.
    ///
    /// Empty query is only supported if `file_ids` are provided. In this case, the
    /// answer will be generated based on those context files.
    public var query: Query? = nil

    /// Optional. The session to use for the request. If specified, the assistant
    /// has access to the session history, and the query and the answer are stored
    /// there.
    ///
    /// If `-` is specified as the session ID, or it is left empty, then a new
    /// session is created with an automatically generated ID.
    ///
    /// Format:
    /// `projects/{project}/locations/{location}/collections/{collection}/engines/{engine}/sessions/{session}`
    public var session: Swift.String = Swift.String()

    /// Optional. Information about the user initiating the query.
    public var userMetadata: AssistUserMetadata? = nil

    /// Optional. Specification of agents that are used to serve the request.
    public var agentsSpec: StreamAssistRequest.AgentsSpec? = nil

    /// Optional. Specification of tools that are used to serve the request.
    public var toolsSpec: StreamAssistRequest.ToolsSpec? = nil

    /// Optional. Specification of the generation configuration for the request.
    public var generationSpec: StreamAssistRequest.GenerationSpec? = nil

    @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

    /// Initialize a new instance of `StreamAssistRequest`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = StreamAssistRequest().with { $0.name = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    private struct CodingKeys: CodingKey {
      var stringValue: Swift.String
      var intValue: Swift.Int? { nil }
      init(stringValue: Swift.String) { self.stringValue = stringValue }
      init?(intValue: Swift.Int) { nil }

      static let name = CodingKeys(stringValue: "name")
      static let query = CodingKeys(stringValue: "query")
      static let session = CodingKeys(stringValue: "session")
      static let userMetadata = CodingKeys(stringValue: "userMetadata")
      static let agentsSpec = CodingKeys(stringValue: "agentsSpec")
      static let toolsSpec = CodingKeys(stringValue: "toolsSpec")
      static let generationSpec = CodingKeys(stringValue: "generationSpec")

      static let _knownKeys: Set<Swift.String> = [
        "name",
        "query",
        "session",
        "userMetadata",
        "agentsSpec",
        "toolsSpec",
        "generationSpec",
      ]
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.container(keyedBy: CodingKeys.self)
      if let value = try container.decodeIfPresent(Swift.String.self, forKey: .name) {
        self.name = value
      }
      self.query = try container.decodeIfPresent(Query.self, forKey: .query)
      if let value = try container.decodeIfPresent(Swift.String.self, forKey: .session) {
        self.session = value
      }
      self.userMetadata = try container.decodeIfPresent(
        AssistUserMetadata.self, forKey: .userMetadata)
      self.agentsSpec = try container.decodeIfPresent(
        StreamAssistRequest.AgentsSpec.self, forKey: .agentsSpec)
      self.toolsSpec = try container.decodeIfPresent(
        StreamAssistRequest.ToolsSpec.self, forKey: .toolsSpec)
      self.generationSpec = try container.decodeIfPresent(
        StreamAssistRequest.GenerationSpec.self, forKey: .generationSpec)
      for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
        self._unknownFields.json[key.stringValue] = try container.decode(
          GoogleWKT.Value.self, forKey: key)
      }
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.container(keyedBy: CodingKeys.self)
      try container.encode(self.name, forKey: .name)
      try container.encodeIfPresent(self.query, forKey: .query)
      try container.encode(self.session, forKey: .session)
      try container.encodeIfPresent(self.userMetadata, forKey: .userMetadata)
      try container.encodeIfPresent(self.agentsSpec, forKey: .agentsSpec)
      try container.encodeIfPresent(self.toolsSpec, forKey: .toolsSpec)
      try container.encodeIfPresent(self.generationSpec, forKey: .generationSpec)
      for (key, value) in self._unknownFields.json {
        try container.encode(value, forKey: CodingKeys(stringValue: key))
      }
    }

    /// Specification of agents that are used to serve the request.
    public struct AgentsSpec: Codable, Equatable, GoogleWKT._AnyPackable,
      Sendable
    {
      /// Optional. Specification of agents that are used to serve the request.
      public var agentSpecs: [StreamAssistRequest.AgentsSpec.AgentSpec] = []

      @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

      /// Initialize a new instance of `AgentsSpec`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = AgentsSpec().with { $0.agentSpecs = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      private struct CodingKeys: CodingKey {
        var stringValue: Swift.String
        var intValue: Swift.Int? { nil }
        init(stringValue: Swift.String) { self.stringValue = stringValue }
        init?(intValue: Swift.Int) { nil }

        static let agentSpecs = CodingKeys(stringValue: "agentSpecs")

        static let _knownKeys: Set<Swift.String> = [
          "agentSpecs"
        ]
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        if let value = try container.decodeIfPresent(
          [StreamAssistRequest.AgentsSpec.AgentSpec].self, forKey: .agentSpecs)
        {
          self.agentSpecs = value
        }
        for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
          self._unknownFields.json[key.stringValue] = try container.decode(
            GoogleWKT.Value.self, forKey: key)
        }
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.agentSpecs, forKey: .agentSpecs)
        for (key, value) in self._unknownFields.json {
          try container.encode(value, forKey: CodingKeys(stringValue: key))
        }
      }

      /// Specification of an agent.
      public struct AgentSpec: Codable, Equatable, GoogleWKT._AnyPackable,
        Sendable
      {
        /// Required. ID to identify the agent resource serving the request.
        ///
        /// This field must conform to
        /// [RFC-1034](https://tools.ietf.org/html/rfc1034)
        /// with a length limit of 63 characters.
        public var agentId: Swift.String = Swift.String()

        @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

        /// Initialize a new instance of `AgentSpec`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = AgentSpec().with { $0.agentId = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        private struct CodingKeys: CodingKey {
          var stringValue: Swift.String
          var intValue: Swift.Int? { nil }
          init(stringValue: Swift.String) { self.stringValue = stringValue }
          init?(intValue: Swift.Int) { nil }

          static let agentId = CodingKeys(stringValue: "agentId")

          static let _knownKeys: Set<Swift.String> = [
            "agentId"
          ]
        }

        public init(from decoder: Decoder) throws {
          let container = try decoder.container(keyedBy: CodingKeys.self)
          if let value = try container.decodeIfPresent(Swift.String.self, forKey: .agentId) {
            self.agentId = value
          }
          for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
            self._unknownFields.json[key.stringValue] = try container.decode(
              GoogleWKT.Value.self, forKey: key)
          }
        }

        public func encode(to encoder: Encoder) throws {
          var container = encoder.container(keyedBy: CodingKeys.self)
          try container.encode(self.agentId, forKey: .agentId)
          for (key, value) in self._unknownFields.json {
            try container.encode(value, forKey: CodingKeys(stringValue: key))
          }
        }

        public static var _anyTypeUrl: Swift.String {
          return
            "type.googleapis.com/google.cloud.discoveryengine.v1.StreamAssistRequest.AgentsSpec.AgentSpec"
        }
        public init(fromAny any: GoogleWKT.`Any`) throws {
          self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleWKT.Struct {
          return try GoogleWKT._slowAnySerialize(message: self)
        }
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.cloud.discoveryengine.v1.StreamAssistRequest.AgentsSpec"
      }
      public init(fromAny any: GoogleWKT.`Any`) throws {
        self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleWKT.Struct {
        return try GoogleWKT._slowAnySerialize(message: self)
      }
    }

    /// Specification of tools that are used to serve the request.
    public struct ToolsSpec: Codable, Equatable, GoogleWKT._AnyPackable,
      Sendable
    {
      /// Optional. Specification of the Vertex AI Search tool.
      public var vertexAiSearchSpec: StreamAssistRequest.ToolsSpec.VertexAiSearchSpec? = nil

      /// Optional. Specification of the web grounding tool.
      /// If field is present, enables grounding with web search. Works only if
      /// [Assistant.web_grounding_type][google.cloud.discoveryengine.v1.Assistant.web_grounding_type]
      /// is [WEB_GROUNDING_TYPE_GOOGLE_SEARCH][] or
      /// [WEB_GROUNDING_TYPE_ENTERPRISE_WEB_SEARCH][].
      public var webGroundingSpec: StreamAssistRequest.ToolsSpec.WebGroundingSpec? = nil

      /// Optional. Specification of the image generation tool.
      public var imageGenerationSpec: StreamAssistRequest.ToolsSpec.ImageGenerationSpec? = nil

      /// Optional. Specification of the video generation tool.
      public var videoGenerationSpec: StreamAssistRequest.ToolsSpec.VideoGenerationSpec? = nil

      @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

      /// Initialize a new instance of `ToolsSpec`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = ToolsSpec().with { $0.vertexAiSearchSpec = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      private struct CodingKeys: CodingKey {
        var stringValue: Swift.String
        var intValue: Swift.Int? { nil }
        init(stringValue: Swift.String) { self.stringValue = stringValue }
        init?(intValue: Swift.Int) { nil }

        static let vertexAiSearchSpec = CodingKeys(stringValue: "vertexAiSearchSpec")
        static let webGroundingSpec = CodingKeys(stringValue: "webGroundingSpec")
        static let imageGenerationSpec = CodingKeys(stringValue: "imageGenerationSpec")
        static let videoGenerationSpec = CodingKeys(stringValue: "videoGenerationSpec")

        static let _knownKeys: Set<Swift.String> = [
          "vertexAiSearchSpec",
          "webGroundingSpec",
          "imageGenerationSpec",
          "videoGenerationSpec",
        ]
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.vertexAiSearchSpec = try container.decodeIfPresent(
          StreamAssistRequest.ToolsSpec.VertexAiSearchSpec.self, forKey: .vertexAiSearchSpec)
        self.webGroundingSpec = try container.decodeIfPresent(
          StreamAssistRequest.ToolsSpec.WebGroundingSpec.self, forKey: .webGroundingSpec)
        self.imageGenerationSpec = try container.decodeIfPresent(
          StreamAssistRequest.ToolsSpec.ImageGenerationSpec.self, forKey: .imageGenerationSpec)
        self.videoGenerationSpec = try container.decodeIfPresent(
          StreamAssistRequest.ToolsSpec.VideoGenerationSpec.self, forKey: .videoGenerationSpec)
        for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
          self._unknownFields.json[key.stringValue] = try container.decode(
            GoogleWKT.Value.self, forKey: key)
        }
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(self.vertexAiSearchSpec, forKey: .vertexAiSearchSpec)
        try container.encodeIfPresent(self.webGroundingSpec, forKey: .webGroundingSpec)
        try container.encodeIfPresent(self.imageGenerationSpec, forKey: .imageGenerationSpec)
        try container.encodeIfPresent(self.videoGenerationSpec, forKey: .videoGenerationSpec)
        for (key, value) in self._unknownFields.json {
          try container.encode(value, forKey: CodingKeys(stringValue: key))
        }
      }

      /// Specification of the Vertex AI Search tool.
      public struct VertexAiSearchSpec: Codable, Equatable, GoogleWKT._AnyPackable,
        Sendable
      {
        /// Optional. Specs defining
        /// [DataStore][google.cloud.discoveryengine.v1.DataStore]s to filter on in
        /// a search call and configurations for those data stores. This is only
        /// considered for [Engine][google.cloud.discoveryengine.v1.Engine]s with
        /// multiple data stores.
        ///
        /// [google.cloud.discoveryengine.v1.DataStore]: <doc:DataStore>
        /// [google.cloud.discoveryengine.v1.Engine]: <doc:Engine>
        public var dataStoreSpecs: [SearchRequest.DataStoreSpec] = []

        /// Optional. The filter syntax consists of an expression language for
        /// constructing a predicate from one or more fields of the documents being
        /// filtered. Filter expression is case-sensitive.
        ///
        /// If this field is unrecognizable, an  `INVALID_ARGUMENT`  is returned.
        ///
        /// Filtering in Vertex AI Search is done by mapping the LHS filter key to
        /// a key property defined in the Vertex AI Search backend -- this mapping
        /// is defined by the customer in their schema. For example a media
        /// customer might have a field 'name' in their schema. In this case the
        /// filter would look like this: filter --> name:'ANY("king kong")'
        ///
        /// For more information about filtering including syntax and filter
        /// operators, see
        /// [Filter](https://cloud.google.com/generative-ai-app-builder/docs/filter-search-metadata)
        public var filter: Swift.String = Swift.String()

        @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

        /// Initialize a new instance of `VertexAiSearchSpec`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = VertexAiSearchSpec().with { $0.dataStoreSpecs = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        private struct CodingKeys: CodingKey {
          var stringValue: Swift.String
          var intValue: Swift.Int? { nil }
          init(stringValue: Swift.String) { self.stringValue = stringValue }
          init?(intValue: Swift.Int) { nil }

          static let dataStoreSpecs = CodingKeys(stringValue: "dataStoreSpecs")
          static let filter = CodingKeys(stringValue: "filter")

          static let _knownKeys: Set<Swift.String> = [
            "dataStoreSpecs",
            "filter",
          ]
        }

        public init(from decoder: Decoder) throws {
          let container = try decoder.container(keyedBy: CodingKeys.self)
          if let value = try container.decodeIfPresent(
            [SearchRequest.DataStoreSpec].self, forKey: .dataStoreSpecs)
          {
            self.dataStoreSpecs = value
          }
          if let value = try container.decodeIfPresent(Swift.String.self, forKey: .filter) {
            self.filter = value
          }
          for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
            self._unknownFields.json[key.stringValue] = try container.decode(
              GoogleWKT.Value.self, forKey: key)
          }
        }

        public func encode(to encoder: Encoder) throws {
          var container = encoder.container(keyedBy: CodingKeys.self)
          try container.encode(self.dataStoreSpecs, forKey: .dataStoreSpecs)
          try container.encode(self.filter, forKey: .filter)
          for (key, value) in self._unknownFields.json {
            try container.encode(value, forKey: CodingKeys(stringValue: key))
          }
        }

        public static var _anyTypeUrl: Swift.String {
          return
            "type.googleapis.com/google.cloud.discoveryengine.v1.StreamAssistRequest.ToolsSpec.VertexAiSearchSpec"
        }
        public init(fromAny any: GoogleWKT.`Any`) throws {
          self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleWKT.Struct {
          return try GoogleWKT._slowAnySerialize(message: self)
        }
      }

      /// Specification of the web grounding tool.
      public struct WebGroundingSpec: Codable, Equatable, GoogleWKT._AnyPackable,
        Sendable
      {
        @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

        /// Initialize a new instance of `WebGroundingSpec`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = WebGroundingSpec().with { $0.<placeholder> = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        private struct CodingKeys: CodingKey {
          var stringValue: Swift.String
          var intValue: Swift.Int? { nil }
          init(stringValue: Swift.String) { self.stringValue = stringValue }
          init?(intValue: Swift.Int) { nil }

          static let _knownKeys: Set<Swift.String> = []
        }

        public init(from decoder: Decoder) throws {
          let container = try decoder.container(keyedBy: CodingKeys.self)
          for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
            self._unknownFields.json[key.stringValue] = try container.decode(
              GoogleWKT.Value.self, forKey: key)
          }
        }

        public func encode(to encoder: Encoder) throws {
          var container = encoder.container(keyedBy: CodingKeys.self)
          for (key, value) in self._unknownFields.json {
            try container.encode(value, forKey: CodingKeys(stringValue: key))
          }
        }

        public static var _anyTypeUrl: Swift.String {
          return
            "type.googleapis.com/google.cloud.discoveryengine.v1.StreamAssistRequest.ToolsSpec.WebGroundingSpec"
        }
        public init(fromAny any: GoogleWKT.`Any`) throws {
          self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleWKT.Struct {
          return try GoogleWKT._slowAnySerialize(message: self)
        }
      }

      /// Specification of the image generation tool.
      public struct ImageGenerationSpec: Codable, Equatable, GoogleWKT._AnyPackable,
        Sendable
      {
        @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

        /// Initialize a new instance of `ImageGenerationSpec`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = ImageGenerationSpec().with { $0.<placeholder> = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        private struct CodingKeys: CodingKey {
          var stringValue: Swift.String
          var intValue: Swift.Int? { nil }
          init(stringValue: Swift.String) { self.stringValue = stringValue }
          init?(intValue: Swift.Int) { nil }

          static let _knownKeys: Set<Swift.String> = []
        }

        public init(from decoder: Decoder) throws {
          let container = try decoder.container(keyedBy: CodingKeys.self)
          for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
            self._unknownFields.json[key.stringValue] = try container.decode(
              GoogleWKT.Value.self, forKey: key)
          }
        }

        public func encode(to encoder: Encoder) throws {
          var container = encoder.container(keyedBy: CodingKeys.self)
          for (key, value) in self._unknownFields.json {
            try container.encode(value, forKey: CodingKeys(stringValue: key))
          }
        }

        public static var _anyTypeUrl: Swift.String {
          return
            "type.googleapis.com/google.cloud.discoveryengine.v1.StreamAssistRequest.ToolsSpec.ImageGenerationSpec"
        }
        public init(fromAny any: GoogleWKT.`Any`) throws {
          self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleWKT.Struct {
          return try GoogleWKT._slowAnySerialize(message: self)
        }
      }

      /// Specification of the video generation tool.
      public struct VideoGenerationSpec: Codable, Equatable, GoogleWKT._AnyPackable,
        Sendable
      {
        @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

        /// Initialize a new instance of `VideoGenerationSpec`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = VideoGenerationSpec().with { $0.<placeholder> = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        private struct CodingKeys: CodingKey {
          var stringValue: Swift.String
          var intValue: Swift.Int? { nil }
          init(stringValue: Swift.String) { self.stringValue = stringValue }
          init?(intValue: Swift.Int) { nil }

          static let _knownKeys: Set<Swift.String> = []
        }

        public init(from decoder: Decoder) throws {
          let container = try decoder.container(keyedBy: CodingKeys.self)
          for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
            self._unknownFields.json[key.stringValue] = try container.decode(
              GoogleWKT.Value.self, forKey: key)
          }
        }

        public func encode(to encoder: Encoder) throws {
          var container = encoder.container(keyedBy: CodingKeys.self)
          for (key, value) in self._unknownFields.json {
            try container.encode(value, forKey: CodingKeys(stringValue: key))
          }
        }

        public static var _anyTypeUrl: Swift.String {
          return
            "type.googleapis.com/google.cloud.discoveryengine.v1.StreamAssistRequest.ToolsSpec.VideoGenerationSpec"
        }
        public init(fromAny any: GoogleWKT.`Any`) throws {
          self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleWKT.Struct {
          return try GoogleWKT._slowAnySerialize(message: self)
        }
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.cloud.discoveryengine.v1.StreamAssistRequest.ToolsSpec"
      }
      public init(fromAny any: GoogleWKT.`Any`) throws {
        self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleWKT.Struct {
        return try GoogleWKT._slowAnySerialize(message: self)
      }
    }

    /// Assistant generation specification for the request.
    /// This allows to override the default generation configuration at the engine
    /// level.
    public struct GenerationSpec: Codable, Equatable, GoogleWKT._AnyPackable,
      Sendable
    {
      /// Optional. The Vertex AI model_id used for the generative model. If not
      /// set, the default Assistant model will be used.
      public var modelId: Swift.String = Swift.String()

      @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

      /// Initialize a new instance of `GenerationSpec`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = GenerationSpec().with { $0.modelId = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      private struct CodingKeys: CodingKey {
        var stringValue: Swift.String
        var intValue: Swift.Int? { nil }
        init(stringValue: Swift.String) { self.stringValue = stringValue }
        init?(intValue: Swift.Int) { nil }

        static let modelId = CodingKeys(stringValue: "modelId")

        static let _knownKeys: Set<Swift.String> = [
          "modelId"
        ]
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        if let value = try container.decodeIfPresent(Swift.String.self, forKey: .modelId) {
          self.modelId = value
        }
        for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
          self._unknownFields.json[key.stringValue] = try container.decode(
            GoogleWKT.Value.self, forKey: key)
        }
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.modelId, forKey: .modelId)
        for (key, value) in self._unknownFields.json {
          try container.encode(value, forKey: CodingKeys(stringValue: key))
        }
      }

      public static var _anyTypeUrl: Swift.String {
        return
          "type.googleapis.com/google.cloud.discoveryengine.v1.StreamAssistRequest.GenerationSpec"
      }
      public init(fromAny any: GoogleWKT.`Any`) throws {
        self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleWKT.Struct {
        return try GoogleWKT._slowAnySerialize(message: self)
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.discoveryengine.v1.StreamAssistRequest"
    }
    public init(fromAny any: GoogleWKT.`Any`) throws {
      self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleWKT.Struct {
      return try GoogleWKT._slowAnySerialize(message: self)
    }
  }
#endif
