// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

#if ConversationalSearchService || DocumentService || RecommendationService || SearchService
  import Foundation
  import GoogleCloudWkt

  /// Principal identifier of a user or a group.
  public struct Principal: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Union field principal. Principal can be a user or a group.
    public var principal: OneOf_Principal? = nil

    /// Initialize a new instance of `Principal`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = Principal().with { $0.userId = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    private enum CodingKeys: Swift.String, CodingKey {
      case userId = "userId"
      case groupId = "groupId"
      case externalEntityId = "externalEntityId"
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.container(keyedBy: CodingKeys.self)

      var principal: OneOf_Principal? = nil
      let principalCheckAndSet = {
        if principal != nil {
          throw DecodingError.dataCorrupted(
            DecodingError.Context(
              codingPath: decoder.codingPath,
              debugDescription: "Multiple values set for oneof 'principal'"))
        }
        principal = $0
      }
      if let userId = try container.decodeIfPresent(Swift.String.self, forKey: .userId) {
        try principalCheckAndSet(.userId(userId))
      }
      if let groupId = try container.decodeIfPresent(Swift.String.self, forKey: .groupId) {
        try principalCheckAndSet(.groupId(groupId))
      }
      if let externalEntityId = try container.decodeIfPresent(
        Swift.String.self, forKey: .externalEntityId)
      {
        try principalCheckAndSet(.externalEntityId(externalEntityId))
      }
      self.principal = principal
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.container(keyedBy: CodingKeys.self)

      if let choice = self.principal {
        switch choice {
        case .userId(let value):
          try container.encode(value, forKey: .userId)
        case .groupId(let value):
          try container.encode(value, forKey: .groupId)
        case .externalEntityId(let value):
          try container.encode(value, forKey: .externalEntityId)
        }
      }
    }

    /// Union field principal. Principal can be a user or a group.
    public enum OneOf_Principal: Codable, Equatable, Sendable {
      /// User identifier.
      /// For Google Workspace user account, user_id should be the google workspace
      /// user email.
      /// For non-google identity provider user account, user_id is the mapped user
      /// identifier configured during the workforcepool config.
      case userId(Swift.String)
      /// Group identifier.
      /// For Google Workspace user account, group_id should be the google
      /// workspace group email.
      /// For non-google identity provider user account, group_id is the mapped
      /// group identifier configured during the workforcepool config.
      case groupId(Swift.String)
      /// For 3P application identities which are not present in the customer
      /// identity provider.
      case externalEntityId(Swift.String)
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.discoveryengine.v1.Principal"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }
#endif
