// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

#if GroundedGenerationService
  import Foundation
  @_spi(GoogleCloudInternal) import GoogleWKT

  /// Response for the `GenerateGroundedContent` method.
  public struct GenerateGroundedContentResponse: Codable, Equatable, GoogleWKT._AnyPackable,
    Sendable
  {
    /// Generated candidates.
    public var candidates: [GenerateGroundedContentResponse.Candidate] = []

    @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

    /// Initialize a new instance of `GenerateGroundedContentResponse`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = GenerateGroundedContentResponse().with { $0.candidates = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    private struct CodingKeys: CodingKey {
      var stringValue: Swift.String
      var intValue: Swift.Int? { nil }
      init(stringValue: Swift.String) { self.stringValue = stringValue }
      init?(intValue: Swift.Int) { nil }

      static let candidates = CodingKeys(stringValue: "candidates")

      static let _knownKeys: Set<Swift.String> = [
        "candidates"
      ]
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.container(keyedBy: CodingKeys.self)
      if let value = try container.decodeIfPresent(
        [GenerateGroundedContentResponse.Candidate].self, forKey: .candidates)
      {
        self.candidates = value
      }
      for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
        self._unknownFields.json[key.stringValue] = try container.decode(
          GoogleWKT.Value.self, forKey: key)
      }
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.container(keyedBy: CodingKeys.self)
      try container.encode(self.candidates, forKey: .candidates)
      for (key, value) in self._unknownFields.json {
        try container.encode(value, forKey: CodingKeys(stringValue: key))
      }
    }

    /// A response candidate generated from the model.
    public struct Candidate: Codable, Equatable, GoogleWKT._AnyPackable,
      Sendable
    {
      /// Index of the candidate.
      public var index: Swift.Int32 = Swift.Int32()

      /// Content of the candidate.
      public var content: GroundedGenerationContent? = nil

      /// The overall grounding score for the candidate, in the range of [0, 1].
      public var groundingScore: Swift.Float? = nil

      /// Grounding metadata for the generated content.
      public var groundingMetadata: GenerateGroundedContentResponse.Candidate.GroundingMetadata? =
        nil

      @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

      /// Initialize a new instance of `Candidate`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = Candidate().with { $0.index = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      private struct CodingKeys: CodingKey {
        var stringValue: Swift.String
        var intValue: Swift.Int? { nil }
        init(stringValue: Swift.String) { self.stringValue = stringValue }
        init?(intValue: Swift.Int) { nil }

        static let index = CodingKeys(stringValue: "index")
        static let content = CodingKeys(stringValue: "content")
        static let groundingScore = CodingKeys(stringValue: "groundingScore")
        static let groundingMetadata = CodingKeys(stringValue: "groundingMetadata")

        static let _knownKeys: Set<Swift.String> = [
          "index",
          "content",
          "groundingScore",
          "groundingMetadata",
        ]
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        if let value = try container.decodeIfPresent(Swift.Int32.self, forKey: .index) {
          self.index = value
        }
        self.content = try container.decodeIfPresent(
          GroundedGenerationContent.self, forKey: .content)
        self.groundingScore = try container.decodeIfPresent(
          Swift.Float.self, forKey: .groundingScore)
        self.groundingMetadata = try container.decodeIfPresent(
          GenerateGroundedContentResponse.Candidate.GroundingMetadata.self,
          forKey: .groundingMetadata)
        for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
          self._unknownFields.json[key.stringValue] = try container.decode(
            GoogleWKT.Value.self, forKey: key)
        }
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.index, forKey: .index)
        try container.encodeIfPresent(self.content, forKey: .content)
        try container.encodeIfPresent(self.groundingScore, forKey: .groundingScore)
        try container.encodeIfPresent(self.groundingMetadata, forKey: .groundingMetadata)
        for (key, value) in self._unknownFields.json {
          try container.encode(value, forKey: CodingKeys(stringValue: key))
        }
      }

      /// Citation for the generated content.
      public struct GroundingMetadata: Codable, Equatable, GoogleWKT._AnyPackable,
        Sendable
      {
        /// Retrieval metadata to provide an understanding in the
        /// retrieval steps performed by the model. There can be multiple such
        /// messages which can correspond to different parts of the retrieval. This
        /// is a mechanism used to ensure transparency to our users.
        public var retrievalMetadata:
          [GenerateGroundedContentResponse.Candidate.GroundingMetadata.RetrievalMetadata] = []

        /// List of chunks to be attributed across all claims in the candidate.
        /// These are derived from the grounding sources supplied in the request.
        public var supportChunks: [FactChunk] = []

        /// Web search queries for the following-up web search.
        public var webSearchQueries: [Swift.String] = []

        /// Google search entry for the following-up web searches.
        public var searchEntryPoint:
          GenerateGroundedContentResponse.Candidate.GroundingMetadata.SearchEntryPoint? = nil

        /// GroundingSupport across all claims in the answer candidate.
        /// An support to a fact indicates that the claim is supported by
        /// the fact.
        public var groundingSupport:
          [GenerateGroundedContentResponse.Candidate.GroundingMetadata.GroundingSupport] = []

        /// Images from the web search.
        public var images:
          [GenerateGroundedContentResponse.Candidate.GroundingMetadata.ImageMetadata] = []

        @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

        /// Initialize a new instance of `GroundingMetadata`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = GroundingMetadata().with { $0.retrievalMetadata = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        private struct CodingKeys: CodingKey {
          var stringValue: Swift.String
          var intValue: Swift.Int? { nil }
          init(stringValue: Swift.String) { self.stringValue = stringValue }
          init?(intValue: Swift.Int) { nil }

          static let retrievalMetadata = CodingKeys(stringValue: "retrievalMetadata")
          static let supportChunks = CodingKeys(stringValue: "supportChunks")
          static let webSearchQueries = CodingKeys(stringValue: "webSearchQueries")
          static let searchEntryPoint = CodingKeys(stringValue: "searchEntryPoint")
          static let groundingSupport = CodingKeys(stringValue: "groundingSupport")
          static let images = CodingKeys(stringValue: "images")

          static let _knownKeys: Set<Swift.String> = [
            "retrievalMetadata",
            "supportChunks",
            "webSearchQueries",
            "searchEntryPoint",
            "groundingSupport",
            "images",
          ]
        }

        public init(from decoder: Decoder) throws {
          let container = try decoder.container(keyedBy: CodingKeys.self)
          if let value = try container.decodeIfPresent(
            [GenerateGroundedContentResponse.Candidate.GroundingMetadata.RetrievalMetadata].self,
            forKey: .retrievalMetadata)
          {
            self.retrievalMetadata = value
          }
          if let value = try container.decodeIfPresent([FactChunk].self, forKey: .supportChunks) {
            self.supportChunks = value
          }
          if let value = try container.decodeIfPresent(
            [Swift.String].self, forKey: .webSearchQueries)
          {
            self.webSearchQueries = value
          }
          self.searchEntryPoint = try container.decodeIfPresent(
            GenerateGroundedContentResponse.Candidate.GroundingMetadata.SearchEntryPoint.self,
            forKey: .searchEntryPoint)
          if let value = try container.decodeIfPresent(
            [GenerateGroundedContentResponse.Candidate.GroundingMetadata.GroundingSupport].self,
            forKey: .groundingSupport)
          {
            self.groundingSupport = value
          }
          if let value = try container.decodeIfPresent(
            [GenerateGroundedContentResponse.Candidate.GroundingMetadata.ImageMetadata].self,
            forKey: .images)
          {
            self.images = value
          }
          for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
            self._unknownFields.json[key.stringValue] = try container.decode(
              GoogleWKT.Value.self, forKey: key)
          }
        }

        public func encode(to encoder: Encoder) throws {
          var container = encoder.container(keyedBy: CodingKeys.self)
          try container.encode(self.retrievalMetadata, forKey: .retrievalMetadata)
          try container.encode(self.supportChunks, forKey: .supportChunks)
          try container.encode(self.webSearchQueries, forKey: .webSearchQueries)
          try container.encodeIfPresent(self.searchEntryPoint, forKey: .searchEntryPoint)
          try container.encode(self.groundingSupport, forKey: .groundingSupport)
          try container.encode(self.images, forKey: .images)
          for (key, value) in self._unknownFields.json {
            try container.encode(value, forKey: CodingKeys(stringValue: key))
          }
        }

        /// Describes the metadata associated with a retrieval step.
        public struct RetrievalMetadata: Codable, Equatable, GoogleWKT._AnyPackable,
          Sendable
        {
          /// Describes the source to which the metadata is referring to.
          public var source:
            GenerateGroundedContentResponse.Candidate.GroundingMetadata.RetrievalMetadata.Source =
              GenerateGroundedContentResponse.Candidate.GroundingMetadata.RetrievalMetadata.Source()

          /// Metadata for dynamic retrieval.
          public var dynamicRetrievalMetadata:
            GenerateGroundedContentResponse.Candidate.GroundingMetadata.DynamicRetrievalMetadata? =
              nil

          @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

          /// Initialize a new instance of `RetrievalMetadata`.
          public init() {}

          /// Use `config` to return a new instance of this object, with some fields updated.
          ///
          /// Commonly used to initialize the value, for example:
          ///
          /// ```
          /// let value = RetrievalMetadata().with { $0.source = ... }
          /// ```
          public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
            var copy = self
            try config(&copy)
            return copy
          }

          private struct CodingKeys: CodingKey {
            var stringValue: Swift.String
            var intValue: Swift.Int? { nil }
            init(stringValue: Swift.String) { self.stringValue = stringValue }
            init?(intValue: Swift.Int) { nil }

            static let source = CodingKeys(stringValue: "source")
            static let dynamicRetrievalMetadata = CodingKeys(
              stringValue: "dynamicRetrievalMetadata")

            static let _knownKeys: Set<Swift.String> = [
              "source",
              "dynamicRetrievalMetadata",
            ]
          }

          public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if let value = try container.decodeIfPresent(
              GenerateGroundedContentResponse.Candidate.GroundingMetadata.RetrievalMetadata.Source
                .self, forKey: .source)
            {
              self.source = value
            }
            self.dynamicRetrievalMetadata = try container.decodeIfPresent(
              GenerateGroundedContentResponse.Candidate.GroundingMetadata.DynamicRetrievalMetadata
                .self, forKey: .dynamicRetrievalMetadata)
            for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
              self._unknownFields.json[key.stringValue] = try container.decode(
                GoogleWKT.Value.self, forKey: key)
            }
          }

          public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.source, forKey: .source)
            try container.encodeIfPresent(
              self.dynamicRetrievalMetadata, forKey: .dynamicRetrievalMetadata)
            for (key, value) in self._unknownFields.json {
              try container.encode(value, forKey: CodingKeys(stringValue: key))
            }
          }

          /// Describes the source to which the metadata is associated to.
          public enum Source: Codable, Equatable, Sendable {
            /// Unspecified source.
            case unspecified
            /// Vertex AI search.
            case vertexAiSearch
            /// User inline provided content.
            case inlineContent
            /// Google Search.
            case googleSearch
            /// Google Maps.
            case googleMaps
            /// Encodes an unknown integer value.
            ///
            /// The most common cause for an unknown values is for the service to send
            /// a value unknown to the library. We recommend you update your library to
            /// the latest version.
            case unknownIntValue(Int)
            /// Encodes an unknown string value.
            ///
            /// The most common cause for an unknown values is for the service to send
            /// a value unknown to the library. We recommend you update your library to
            /// the latest version.
            case unknownStringValue(String)

            public init() {
              self = .unspecified
            }

            /// Returns the integer value associated with the enumeration.
            ///
            /// If the enumeration was initialized with an unknown string value, this returns `nil`.
            public var intValue: Int? {
              switch self {
              case .unspecified: return 0
              case .vertexAiSearch: return 1
              case .inlineContent: return 2
              case .googleSearch: return 3
              case .googleMaps: return 4
              case .unknownIntValue(let v): return v
              case .unknownStringValue: return nil
              }
            }

            /// Returns the string value (or name) associated with the enumeration.
            ///
            /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
            public var stringValue: Swift.String? {
              switch self {
              case .unspecified: return "SOURCE_UNSPECIFIED"
              case .vertexAiSearch: return "VERTEX_AI_SEARCH"
              case .inlineContent: return "INLINE_CONTENT"
              case .googleSearch: return "GOOGLE_SEARCH"
              case .googleMaps: return "GOOGLE_MAPS"
              case .unknownIntValue: return nil
              case .unknownStringValue(let v): return v
              }
            }

            /// Initialize from a string value.
            ///
            /// If the value is unknown, this initializes to [`unknownStringValue`](doc:Source/unknownStringValue(_:)).
            public init(stringValue: Swift.String) {
              switch stringValue {
              case "SOURCE_UNSPECIFIED": self = .unspecified
              case "VERTEX_AI_SEARCH": self = .vertexAiSearch
              case "GOOGLE_SEARCH": self = .googleSearch
              case "INLINE_CONTENT": self = .inlineContent
              case "GOOGLE_MAPS": self = .googleMaps
              default: self = .unknownStringValue(stringValue)
              }
            }

            /// Initialize from an integer value.
            ///
            /// If the value is unknown, this initializes to [`unknownIntValue`](doc:Source/unknownIntValue(_:)).
            public init(intValue: Int) {
              switch intValue {
              case 0: self = .unspecified
              case 1: self = .vertexAiSearch
              case 2: self = .inlineContent
              case 3: self = .googleSearch
              case 4: self = .googleMaps
              default: self = .unknownIntValue(intValue)
              }
            }

            public init(from decoder: Decoder) throws {
              let container = try decoder.singleValueContainer()
              if let v = try? container.decode(Int.self) {
                self.init(intValue: v)
                return
              }
              if let s = try? container.decode(String.self) {
                if let v = Int(s) {
                  self.init(intValue: v)
                } else {
                  self.init(stringValue: s)
                }
                return
              }
              throw DecodingError.dataCorruptedError(
                in: container, debugDescription: "Expected enum value, must be integer or string.")
            }

            public func encode(to encoder: Encoder) throws {
              var container = encoder.singleValueContainer()
              switch self {
              case .unspecified: return try container.encode("SOURCE_UNSPECIFIED")
              case .vertexAiSearch: return try container.encode("VERTEX_AI_SEARCH")
              case .inlineContent: return try container.encode("INLINE_CONTENT")
              case .googleSearch: return try container.encode("GOOGLE_SEARCH")
              case .googleMaps: return try container.encode("GOOGLE_MAPS")
              case .unknownIntValue(let v): return try container.encode(v)
              case .unknownStringValue(let v): return try container.encode(v)
              }
            }
          }

          public static var _anyTypeUrl: Swift.String {
            return
              "type.googleapis.com/google.cloud.discoveryengine.v1.GenerateGroundedContentResponse.Candidate.GroundingMetadata.RetrievalMetadata"
          }
          public init(fromAny any: GoogleWKT.`Any`) throws {
            self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
          }
          public func _pack() throws -> GoogleWKT.Struct {
            return try GoogleWKT._slowAnySerialize(message: self)
          }
        }

        /// Describes the metadata about dynamic retrieval.
        public struct DynamicRetrievalMetadata: Codable, Equatable, GoogleWKT._AnyPackable,
          Sendable
        {
          /// Metadata for the dynamic retrieval predictor.
          public var predictorMetadata:
            GenerateGroundedContentResponse.Candidate.GroundingMetadata
              .DynamicRetrievalPredictorMetadata? = nil

          @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

          /// Initialize a new instance of `DynamicRetrievalMetadata`.
          public init() {}

          /// Use `config` to return a new instance of this object, with some fields updated.
          ///
          /// Commonly used to initialize the value, for example:
          ///
          /// ```
          /// let value = DynamicRetrievalMetadata().with { $0.predictorMetadata = ... }
          /// ```
          public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
            var copy = self
            try config(&copy)
            return copy
          }

          private struct CodingKeys: CodingKey {
            var stringValue: Swift.String
            var intValue: Swift.Int? { nil }
            init(stringValue: Swift.String) { self.stringValue = stringValue }
            init?(intValue: Swift.Int) { nil }

            static let predictorMetadata = CodingKeys(stringValue: "predictorMetadata")

            static let _knownKeys: Set<Swift.String> = [
              "predictorMetadata"
            ]
          }

          public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.predictorMetadata = try container.decodeIfPresent(
              GenerateGroundedContentResponse.Candidate.GroundingMetadata
                .DynamicRetrievalPredictorMetadata.self, forKey: .predictorMetadata)
            for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
              self._unknownFields.json[key.stringValue] = try container.decode(
                GoogleWKT.Value.self, forKey: key)
            }
          }

          public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encodeIfPresent(self.predictorMetadata, forKey: .predictorMetadata)
            for (key, value) in self._unknownFields.json {
              try container.encode(value, forKey: CodingKeys(stringValue: key))
            }
          }

          public static var _anyTypeUrl: Swift.String {
            return
              "type.googleapis.com/google.cloud.discoveryengine.v1.GenerateGroundedContentResponse.Candidate.GroundingMetadata.DynamicRetrievalMetadata"
          }
          public init(fromAny any: GoogleWKT.`Any`) throws {
            self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
          }
          public func _pack() throws -> GoogleWKT.Struct {
            return try GoogleWKT._slowAnySerialize(message: self)
          }
        }

        /// Describes the metadata about the dynamic retrieval predictor.
        public struct DynamicRetrievalPredictorMetadata: Codable, Equatable, GoogleWKT._AnyPackable,
          Sendable
        {
          /// The version of the predictor which was used in dynamic retrieval.
          public var version:
            GenerateGroundedContentResponse.Candidate.GroundingMetadata
              .DynamicRetrievalPredictorMetadata.Version = GenerateGroundedContentResponse.Candidate
              .GroundingMetadata.DynamicRetrievalPredictorMetadata.Version()

          /// The value of the predictor. This should be between [0, 1] where
          /// a value of 0 means that the query would not benefit from grounding,
          /// while a value of 1.0 means that the query would benefit the most.
          /// In between values allow to differentiate between different usefulness
          /// scores for grounding.
          public var prediction: Swift.Float? = nil

          @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

          /// Initialize a new instance of `DynamicRetrievalPredictorMetadata`.
          public init() {}

          /// Use `config` to return a new instance of this object, with some fields updated.
          ///
          /// Commonly used to initialize the value, for example:
          ///
          /// ```
          /// let value = DynamicRetrievalPredictorMetadata().with { $0.version = ... }
          /// ```
          public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
            var copy = self
            try config(&copy)
            return copy
          }

          private struct CodingKeys: CodingKey {
            var stringValue: Swift.String
            var intValue: Swift.Int? { nil }
            init(stringValue: Swift.String) { self.stringValue = stringValue }
            init?(intValue: Swift.Int) { nil }

            static let version = CodingKeys(stringValue: "version")
            static let prediction = CodingKeys(stringValue: "prediction")

            static let _knownKeys: Set<Swift.String> = [
              "version",
              "prediction",
            ]
          }

          public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if let value = try container.decodeIfPresent(
              GenerateGroundedContentResponse.Candidate.GroundingMetadata
                .DynamicRetrievalPredictorMetadata.Version.self, forKey: .version)
            {
              self.version = value
            }
            self.prediction = try container.decodeIfPresent(Swift.Float.self, forKey: .prediction)
            for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
              self._unknownFields.json[key.stringValue] = try container.decode(
                GoogleWKT.Value.self, forKey: key)
            }
          }

          public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.version, forKey: .version)
            try container.encodeIfPresent(self.prediction, forKey: .prediction)
            for (key, value) in self._unknownFields.json {
              try container.encode(value, forKey: CodingKeys(stringValue: key))
            }
          }

          /// The version of the predictor which was used in dynamic retrieval.
          public enum Version: Codable, Equatable, Sendable {
            /// Unspecified version, should never be used.
            case unspecified
            /// The V1 model which is evaluating each source independently.
            case v1Independent
            /// Encodes an unknown integer value.
            ///
            /// The most common cause for an unknown values is for the service to send
            /// a value unknown to the library. We recommend you update your library to
            /// the latest version.
            case unknownIntValue(Int)
            /// Encodes an unknown string value.
            ///
            /// The most common cause for an unknown values is for the service to send
            /// a value unknown to the library. We recommend you update your library to
            /// the latest version.
            case unknownStringValue(String)

            public init() {
              self = .unspecified
            }

            /// Returns the integer value associated with the enumeration.
            ///
            /// If the enumeration was initialized with an unknown string value, this returns `nil`.
            public var intValue: Int? {
              switch self {
              case .unspecified: return 0
              case .v1Independent: return 1
              case .unknownIntValue(let v): return v
              case .unknownStringValue: return nil
              }
            }

            /// Returns the string value (or name) associated with the enumeration.
            ///
            /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
            public var stringValue: Swift.String? {
              switch self {
              case .unspecified: return "VERSION_UNSPECIFIED"
              case .v1Independent: return "V1_INDEPENDENT"
              case .unknownIntValue: return nil
              case .unknownStringValue(let v): return v
              }
            }

            /// Initialize from a string value.
            ///
            /// If the value is unknown, this initializes to [`unknownStringValue`](doc:Version/unknownStringValue(_:)).
            public init(stringValue: Swift.String) {
              switch stringValue {
              case "VERSION_UNSPECIFIED": self = .unspecified
              case "V1_INDEPENDENT": self = .v1Independent
              default: self = .unknownStringValue(stringValue)
              }
            }

            /// Initialize from an integer value.
            ///
            /// If the value is unknown, this initializes to [`unknownIntValue`](doc:Version/unknownIntValue(_:)).
            public init(intValue: Int) {
              switch intValue {
              case 0: self = .unspecified
              case 1: self = .v1Independent
              default: self = .unknownIntValue(intValue)
              }
            }

            public init(from decoder: Decoder) throws {
              let container = try decoder.singleValueContainer()
              if let v = try? container.decode(Int.self) {
                self.init(intValue: v)
                return
              }
              if let s = try? container.decode(String.self) {
                if let v = Int(s) {
                  self.init(intValue: v)
                } else {
                  self.init(stringValue: s)
                }
                return
              }
              throw DecodingError.dataCorruptedError(
                in: container, debugDescription: "Expected enum value, must be integer or string.")
            }

            public func encode(to encoder: Encoder) throws {
              var container = encoder.singleValueContainer()
              switch self {
              case .unspecified: return try container.encode("VERSION_UNSPECIFIED")
              case .v1Independent: return try container.encode("V1_INDEPENDENT")
              case .unknownIntValue(let v): return try container.encode(v)
              case .unknownStringValue(let v): return try container.encode(v)
              }
            }
          }

          public static var _anyTypeUrl: Swift.String {
            return
              "type.googleapis.com/google.cloud.discoveryengine.v1.GenerateGroundedContentResponse.Candidate.GroundingMetadata.DynamicRetrievalPredictorMetadata"
          }
          public init(fromAny any: GoogleWKT.`Any`) throws {
            self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
          }
          public func _pack() throws -> GoogleWKT.Struct {
            return try GoogleWKT._slowAnySerialize(message: self)
          }
        }

        /// Google search entry point.
        public struct SearchEntryPoint: Codable, Equatable, GoogleWKT._AnyPackable,
          Sendable
        {
          /// Web content snippet that can be embedded in a web page or an app
          /// webview.
          public var renderedContent: Swift.String = Swift.String()

          /// Base64 encoded JSON representing array of <search term, search url>
          /// tuple.
          public var sdkBlob: Foundation.Data = Foundation.Data()

          @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

          /// Initialize a new instance of `SearchEntryPoint`.
          public init() {}

          /// Use `config` to return a new instance of this object, with some fields updated.
          ///
          /// Commonly used to initialize the value, for example:
          ///
          /// ```
          /// let value = SearchEntryPoint().with { $0.renderedContent = ... }
          /// ```
          public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
            var copy = self
            try config(&copy)
            return copy
          }

          private struct CodingKeys: CodingKey {
            var stringValue: Swift.String
            var intValue: Swift.Int? { nil }
            init(stringValue: Swift.String) { self.stringValue = stringValue }
            init?(intValue: Swift.Int) { nil }

            static let renderedContent = CodingKeys(stringValue: "renderedContent")
            static let sdkBlob = CodingKeys(stringValue: "sdkBlob")

            static let _knownKeys: Set<Swift.String> = [
              "renderedContent",
              "sdkBlob",
            ]
          }

          public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if let value = try container.decodeIfPresent(
              Swift.String.self, forKey: .renderedContent)
            {
              self.renderedContent = value
            }
            if let value = try container.decodeIfPresent(Foundation.Data.self, forKey: .sdkBlob) {
              self.sdkBlob = value
            }
            for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
              self._unknownFields.json[key.stringValue] = try container.decode(
                GoogleWKT.Value.self, forKey: key)
            }
          }

          public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.renderedContent, forKey: .renderedContent)
            try container.encode(self.sdkBlob, forKey: .sdkBlob)
            for (key, value) in self._unknownFields.json {
              try container.encode(value, forKey: CodingKeys(stringValue: key))
            }
          }

          public static var _anyTypeUrl: Swift.String {
            return
              "type.googleapis.com/google.cloud.discoveryengine.v1.GenerateGroundedContentResponse.Candidate.GroundingMetadata.SearchEntryPoint"
          }
          public init(fromAny any: GoogleWKT.`Any`) throws {
            self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
          }
          public func _pack() throws -> GoogleWKT.Struct {
            return try GoogleWKT._slowAnySerialize(message: self)
          }
        }

        /// Grounding info for a claim in the candidate and its support.
        public struct GroundingSupport: Codable, Equatable, GoogleWKT._AnyPackable,
          Sendable
        {
          /// Text for the claim in the candidate. Always provided when a
          /// support is found.
          public var claimText: Swift.String = Swift.String()

          /// A list of indices (into 'support_chunks') specifying the
          /// citations associated with the claim. For instance [1,3,4] means
          /// that support_chunks[1], support_chunks[3],
          /// support_chunks[4] are the chunks attributed to the claim.
          public var supportChunkIndices: [Swift.Int32] = []

          /// A score in the range of [0, 1] describing how grounded is a specific
          /// claim in the support chunks indicated.
          /// Higher value means that the claim is better supported by the chunks.
          public var supportScore: Swift.Float? = nil

          @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

          /// Initialize a new instance of `GroundingSupport`.
          public init() {}

          /// Use `config` to return a new instance of this object, with some fields updated.
          ///
          /// Commonly used to initialize the value, for example:
          ///
          /// ```
          /// let value = GroundingSupport().with { $0.claimText = ... }
          /// ```
          public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
            var copy = self
            try config(&copy)
            return copy
          }

          private struct CodingKeys: CodingKey {
            var stringValue: Swift.String
            var intValue: Swift.Int? { nil }
            init(stringValue: Swift.String) { self.stringValue = stringValue }
            init?(intValue: Swift.Int) { nil }

            static let claimText = CodingKeys(stringValue: "claimText")
            static let supportChunkIndices = CodingKeys(stringValue: "supportChunkIndices")
            static let supportScore = CodingKeys(stringValue: "supportScore")

            static let _knownKeys: Set<Swift.String> = [
              "claimText",
              "supportChunkIndices",
              "supportScore",
            ]
          }

          public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if let value = try container.decodeIfPresent(Swift.String.self, forKey: .claimText) {
              self.claimText = value
            }
            if let value = try container.decodeIfPresent(
              [Swift.Int32].self, forKey: .supportChunkIndices)
            {
              self.supportChunkIndices = value
            }
            self.supportScore = try container.decodeIfPresent(
              Swift.Float.self, forKey: .supportScore)
            for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
              self._unknownFields.json[key.stringValue] = try container.decode(
                GoogleWKT.Value.self, forKey: key)
            }
          }

          public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.claimText, forKey: .claimText)
            try container.encode(self.supportChunkIndices, forKey: .supportChunkIndices)
            try container.encodeIfPresent(self.supportScore, forKey: .supportScore)
            for (key, value) in self._unknownFields.json {
              try container.encode(value, forKey: CodingKeys(stringValue: key))
            }
          }

          public static var _anyTypeUrl: Swift.String {
            return
              "type.googleapis.com/google.cloud.discoveryengine.v1.GenerateGroundedContentResponse.Candidate.GroundingMetadata.GroundingSupport"
          }
          public init(fromAny any: GoogleWKT.`Any`) throws {
            self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
          }
          public func _pack() throws -> GoogleWKT.Struct {
            return try GoogleWKT._slowAnySerialize(message: self)
          }
        }

        /// Metadata about an image from the web search.
        public struct ImageMetadata: Codable, Equatable, GoogleWKT._AnyPackable,
          Sendable
        {
          /// Metadata about the full size image.
          public var image:
            GenerateGroundedContentResponse.Candidate.GroundingMetadata.ImageMetadata.Image? = nil

          /// Metadata about the thumbnail.
          public var thumbnail:
            GenerateGroundedContentResponse.Candidate.GroundingMetadata.ImageMetadata.Image? = nil

          /// The details about the website that the image is from.
          public var source:
            GenerateGroundedContentResponse.Candidate.GroundingMetadata.ImageMetadata.WebsiteInfo? =
              nil

          @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

          /// Initialize a new instance of `ImageMetadata`.
          public init() {}

          /// Use `config` to return a new instance of this object, with some fields updated.
          ///
          /// Commonly used to initialize the value, for example:
          ///
          /// ```
          /// let value = ImageMetadata().with { $0.image = ... }
          /// ```
          public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
            var copy = self
            try config(&copy)
            return copy
          }

          private struct CodingKeys: CodingKey {
            var stringValue: Swift.String
            var intValue: Swift.Int? { nil }
            init(stringValue: Swift.String) { self.stringValue = stringValue }
            init?(intValue: Swift.Int) { nil }

            static let image = CodingKeys(stringValue: "image")
            static let thumbnail = CodingKeys(stringValue: "thumbnail")
            static let source = CodingKeys(stringValue: "source")

            static let _knownKeys: Set<Swift.String> = [
              "image",
              "thumbnail",
              "source",
            ]
          }

          public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.image = try container.decodeIfPresent(
              GenerateGroundedContentResponse.Candidate.GroundingMetadata.ImageMetadata.Image.self,
              forKey: .image)
            self.thumbnail = try container.decodeIfPresent(
              GenerateGroundedContentResponse.Candidate.GroundingMetadata.ImageMetadata.Image.self,
              forKey: .thumbnail)
            self.source = try container.decodeIfPresent(
              GenerateGroundedContentResponse.Candidate.GroundingMetadata.ImageMetadata.WebsiteInfo
                .self, forKey: .source)
            for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
              self._unknownFields.json[key.stringValue] = try container.decode(
                GoogleWKT.Value.self, forKey: key)
            }
          }

          public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encodeIfPresent(self.image, forKey: .image)
            try container.encodeIfPresent(self.thumbnail, forKey: .thumbnail)
            try container.encodeIfPresent(self.source, forKey: .source)
            for (key, value) in self._unknownFields.json {
              try container.encode(value, forKey: CodingKeys(stringValue: key))
            }
          }

          /// Metadata about the website that the image is from.
          public struct WebsiteInfo: Codable, Equatable, GoogleWKT._AnyPackable,
            Sendable
          {
            /// The url of the website.
            public var uri: Swift.String = Swift.String()

            /// The title of the website.
            public var title: Swift.String = Swift.String()

            @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

            /// Initialize a new instance of `WebsiteInfo`.
            public init() {}

            /// Use `config` to return a new instance of this object, with some fields updated.
            ///
            /// Commonly used to initialize the value, for example:
            ///
            /// ```
            /// let value = WebsiteInfo().with { $0.uri = ... }
            /// ```
            public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
              var copy = self
              try config(&copy)
              return copy
            }

            private struct CodingKeys: CodingKey {
              var stringValue: Swift.String
              var intValue: Swift.Int? { nil }
              init(stringValue: Swift.String) { self.stringValue = stringValue }
              init?(intValue: Swift.Int) { nil }

              static let uri = CodingKeys(stringValue: "uri")
              static let title = CodingKeys(stringValue: "title")

              static let _knownKeys: Set<Swift.String> = [
                "uri",
                "title",
              ]
            }

            public init(from decoder: Decoder) throws {
              let container = try decoder.container(keyedBy: CodingKeys.self)
              if let value = try container.decodeIfPresent(Swift.String.self, forKey: .uri) {
                self.uri = value
              }
              if let value = try container.decodeIfPresent(Swift.String.self, forKey: .title) {
                self.title = value
              }
              for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
                self._unknownFields.json[key.stringValue] = try container.decode(
                  GoogleWKT.Value.self, forKey: key)
              }
            }

            public func encode(to encoder: Encoder) throws {
              var container = encoder.container(keyedBy: CodingKeys.self)
              try container.encode(self.uri, forKey: .uri)
              try container.encode(self.title, forKey: .title)
              for (key, value) in self._unknownFields.json {
                try container.encode(value, forKey: CodingKeys(stringValue: key))
              }
            }

            public static var _anyTypeUrl: Swift.String {
              return
                "type.googleapis.com/google.cloud.discoveryengine.v1.GenerateGroundedContentResponse.Candidate.GroundingMetadata.ImageMetadata.WebsiteInfo"
            }
            public init(fromAny any: GoogleWKT.`Any`) throws {
              self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
            }
            public func _pack() throws -> GoogleWKT.Struct {
              return try GoogleWKT._slowAnySerialize(message: self)
            }
          }

          /// Metadata about the image.
          public struct Image: Codable, Equatable, GoogleWKT._AnyPackable,
            Sendable
          {
            /// The url of the image.
            public var uri: Swift.String = Swift.String()

            /// The width of the image in pixels.
            public var width: Swift.Int32 = Swift.Int32()

            /// The height of the image in pixels.
            public var height: Swift.Int32 = Swift.Int32()

            @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

            /// Initialize a new instance of `Image`.
            public init() {}

            /// Use `config` to return a new instance of this object, with some fields updated.
            ///
            /// Commonly used to initialize the value, for example:
            ///
            /// ```
            /// let value = Image().with { $0.uri = ... }
            /// ```
            public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
              var copy = self
              try config(&copy)
              return copy
            }

            private struct CodingKeys: CodingKey {
              var stringValue: Swift.String
              var intValue: Swift.Int? { nil }
              init(stringValue: Swift.String) { self.stringValue = stringValue }
              init?(intValue: Swift.Int) { nil }

              static let uri = CodingKeys(stringValue: "uri")
              static let width = CodingKeys(stringValue: "width")
              static let height = CodingKeys(stringValue: "height")

              static let _knownKeys: Set<Swift.String> = [
                "uri",
                "width",
                "height",
              ]
            }

            public init(from decoder: Decoder) throws {
              let container = try decoder.container(keyedBy: CodingKeys.self)
              if let value = try container.decodeIfPresent(Swift.String.self, forKey: .uri) {
                self.uri = value
              }
              if let value = try container.decodeIfPresent(Swift.Int32.self, forKey: .width) {
                self.width = value
              }
              if let value = try container.decodeIfPresent(Swift.Int32.self, forKey: .height) {
                self.height = value
              }
              for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
                self._unknownFields.json[key.stringValue] = try container.decode(
                  GoogleWKT.Value.self, forKey: key)
              }
            }

            public func encode(to encoder: Encoder) throws {
              var container = encoder.container(keyedBy: CodingKeys.self)
              try container.encode(self.uri, forKey: .uri)
              try container.encode(self.width, forKey: .width)
              try container.encode(self.height, forKey: .height)
              for (key, value) in self._unknownFields.json {
                try container.encode(value, forKey: CodingKeys(stringValue: key))
              }
            }

            public static var _anyTypeUrl: Swift.String {
              return
                "type.googleapis.com/google.cloud.discoveryengine.v1.GenerateGroundedContentResponse.Candidate.GroundingMetadata.ImageMetadata.Image"
            }
            public init(fromAny any: GoogleWKT.`Any`) throws {
              self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
            }
            public func _pack() throws -> GoogleWKT.Struct {
              return try GoogleWKT._slowAnySerialize(message: self)
            }
          }

          public static var _anyTypeUrl: Swift.String {
            return
              "type.googleapis.com/google.cloud.discoveryengine.v1.GenerateGroundedContentResponse.Candidate.GroundingMetadata.ImageMetadata"
          }
          public init(fromAny any: GoogleWKT.`Any`) throws {
            self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
          }
          public func _pack() throws -> GoogleWKT.Struct {
            return try GoogleWKT._slowAnySerialize(message: self)
          }
        }

        public static var _anyTypeUrl: Swift.String {
          return
            "type.googleapis.com/google.cloud.discoveryengine.v1.GenerateGroundedContentResponse.Candidate.GroundingMetadata"
        }
        public init(fromAny any: GoogleWKT.`Any`) throws {
          self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleWKT.Struct {
          return try GoogleWKT._slowAnySerialize(message: self)
        }
      }

      public static var _anyTypeUrl: Swift.String {
        return
          "type.googleapis.com/google.cloud.discoveryengine.v1.GenerateGroundedContentResponse.Candidate"
      }
      public init(fromAny any: GoogleWKT.`Any`) throws {
        self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleWKT.Struct {
        return try GoogleWKT._slowAnySerialize(message: self)
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.discoveryengine.v1.GenerateGroundedContentResponse"
    }
    public init(fromAny any: GoogleWKT.`Any`) throws {
      self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleWKT.Struct {
      return try GoogleWKT._slowAnySerialize(message: self)
    }
  }
#endif
