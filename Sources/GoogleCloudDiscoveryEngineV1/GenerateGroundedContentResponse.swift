// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

#if GroundedGenerationService
  import Foundation
  @_spi(GoogleCloudInternal) import GoogleCloudWkt

  /// Response for the `GenerateGroundedContent` method.
  public struct GenerateGroundedContentResponse: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Generated candidates.
    public var candidates: [GenerateGroundedContentResponse.Candidate] = []

    /// Initialize a new instance of `GenerateGroundedContentResponse`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = GenerateGroundedContentResponse().with { $0.candidates = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// A response candidate generated from the model.
    public struct Candidate: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// Index of the candidate.
      public var index: Swift.Int32 = Swift.Int32()

      /// Content of the candidate.
      public var content: GroundedGenerationContent? = nil

      /// The overall grounding score for the candidate, in the range of [0, 1].
      public var groundingScore: Swift.Float? = nil

      /// Grounding metadata for the generated content.
      public var groundingMetadata: GenerateGroundedContentResponse.Candidate.GroundingMetadata? =
        nil

      /// Initialize a new instance of `Candidate`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = Candidate().with { $0.index = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      /// Citation for the generated content.
      public struct GroundingMetadata: Codable, Equatable, GoogleCloudWkt._AnyPackable,
        Sendable
      {
        /// Retrieval metadata to provide an understanding in the
        /// retrieval steps performed by the model. There can be multiple such
        /// messages which can correspond to different parts of the retrieval. This
        /// is a mechanism used to ensure transparency to our users.
        public var retrievalMetadata:
          [GenerateGroundedContentResponse.Candidate.GroundingMetadata.RetrievalMetadata] = []

        /// List of chunks to be attributed across all claims in the candidate.
        /// These are derived from the grounding sources supplied in the request.
        public var supportChunks: [FactChunk] = []

        /// Web search queries for the following-up web search.
        public var webSearchQueries: [Swift.String] = []

        /// Google search entry for the following-up web searches.
        public var searchEntryPoint:
          GenerateGroundedContentResponse.Candidate.GroundingMetadata.SearchEntryPoint? = nil

        /// GroundingSupport across all claims in the answer candidate.
        /// An support to a fact indicates that the claim is supported by
        /// the fact.
        public var groundingSupport:
          [GenerateGroundedContentResponse.Candidate.GroundingMetadata.GroundingSupport] = []

        /// Images from the web search.
        public var images:
          [GenerateGroundedContentResponse.Candidate.GroundingMetadata.ImageMetadata] = []

        /// Initialize a new instance of `GroundingMetadata`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = GroundingMetadata().with { $0.retrievalMetadata = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        /// Describes the metadata associated with a retrieval step.
        public struct RetrievalMetadata: Codable, Equatable, GoogleCloudWkt._AnyPackable,
          Sendable
        {
          /// Describes the source to which the metadata is referring to.
          public var source:
            GenerateGroundedContentResponse.Candidate.GroundingMetadata.RetrievalMetadata.Source =
              GenerateGroundedContentResponse.Candidate.GroundingMetadata.RetrievalMetadata.Source()

          /// Metadata for dynamic retrieval.
          public var dynamicRetrievalMetadata:
            GenerateGroundedContentResponse.Candidate.GroundingMetadata.DynamicRetrievalMetadata? =
              nil

          /// Initialize a new instance of `RetrievalMetadata`.
          public init() {}

          /// Use `config` to return a new instance of this object, with some fields updated.
          ///
          /// Commonly used to initialize the value, for example:
          ///
          /// ```
          /// let value = RetrievalMetadata().with { $0.source = ... }
          /// ```
          public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
            var copy = self
            try config(&copy)
            return copy
          }

          /// Describes the source to which the metadata is associated to.
          public enum Source: Codable, Equatable, Sendable {
            /// Unspecified source.
            case unspecified
            /// Vertex AI search.
            case vertexAiSearch
            /// User inline provided content.
            case inlineContent
            /// Google Search.
            case googleSearch
            /// Google Maps.
            case googleMaps
            /// Encodes an unknown integer value.
            ///
            /// The most common cause for an unknown values is for the service to send
            /// a value unknown to the library. We recommend you update your library to
            /// the latest version.
            case unknownIntValue(Int)
            /// Encodes an unknown string value.
            ///
            /// The most common cause for an unknown values is for the service to send
            /// a value unknown to the library. We recommend you update your library to
            /// the latest version.
            case unknownStringValue(String)

            public init() {
              self = .unspecified
            }

            /// Returns the integer value associated with the enumeration.
            ///
            /// If the enumeration was initialized with an unknown string value, this returns `nil`.
            public var intValue: Int? {
              switch self {
              case .unspecified: return 0
              case .vertexAiSearch: return 1
              case .inlineContent: return 2
              case .googleSearch: return 3
              case .googleMaps: return 4
              case .unknownIntValue(let v): return v
              case .unknownStringValue: return nil
              }
            }

            /// Returns the string value (or name) associated with the enumeration.
            ///
            /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
            public var stringValue: Swift.String? {
              switch self {
              case .unspecified: return "SOURCE_UNSPECIFIED"
              case .vertexAiSearch: return "VERTEX_AI_SEARCH"
              case .inlineContent: return "INLINE_CONTENT"
              case .googleSearch: return "GOOGLE_SEARCH"
              case .googleMaps: return "GOOGLE_MAPS"
              case .unknownIntValue: return nil
              case .unknownStringValue(let v): return v
              }
            }

            /// Initialize from a string value.
            ///
            /// If the value is unknown, this initializes to [`unknownStringValue`](doc:Source/unknownStringValue(_:)).
            public init(stringValue: Swift.String) {
              switch stringValue {
              case "SOURCE_UNSPECIFIED": self = .unspecified
              case "VERTEX_AI_SEARCH": self = .vertexAiSearch
              case "GOOGLE_SEARCH": self = .googleSearch
              case "INLINE_CONTENT": self = .inlineContent
              case "GOOGLE_MAPS": self = .googleMaps
              default: self = .unknownStringValue(stringValue)
              }
            }

            /// Initialize from an integer value.
            ///
            /// If the value is unknown, this initializes to [`unknownIntValue`](doc:Source/unknownIntValue(_:)).
            public init(intValue: Int) {
              switch intValue {
              case 0: self = .unspecified
              case 1: self = .vertexAiSearch
              case 2: self = .inlineContent
              case 3: self = .googleSearch
              case 4: self = .googleMaps
              default: self = .unknownIntValue(intValue)
              }
            }

            public init(from decoder: Decoder) throws {
              let container = try decoder.singleValueContainer()
              if let v = try? container.decode(Int.self) {
                self.init(intValue: v)
                return
              }
              if let s = try? container.decode(String.self) {
                if let v = Int(s) {
                  self.init(intValue: v)
                } else {
                  self.init(stringValue: s)
                }
                return
              }
              throw DecodingError.dataCorruptedError(
                in: container, debugDescription: "Expected enum value, must be integer or string.")
            }

            public func encode(to encoder: Encoder) throws {
              var container = encoder.singleValueContainer()
              switch self {
              case .unspecified: return try container.encode(0)
              case .vertexAiSearch: return try container.encode(1)
              case .inlineContent: return try container.encode(2)
              case .googleSearch: return try container.encode(3)
              case .googleMaps: return try container.encode(4)
              case .unknownIntValue(let v): return try container.encode(v)
              case .unknownStringValue(let v): return try container.encode(v)
              }
            }
          }

          public static var _anyTypeUrl: Swift.String {
            return
              "type.googleapis.com/google.cloud.discoveryengine.v1.GenerateGroundedContentResponse.Candidate.GroundingMetadata.RetrievalMetadata"
          }
          public init(fromAny any: GoogleCloudWkt.`Any`) throws {
            self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
          }
          public func _pack() throws -> GoogleCloudWkt.Struct {
            return try GoogleCloudWkt._slowAnySerialize(message: self)
          }
        }

        /// Describes the metadata about dynamic retrieval.
        public struct DynamicRetrievalMetadata: Codable, Equatable, GoogleCloudWkt._AnyPackable,
          Sendable
        {
          /// Metadata for the dynamic retrieval predictor.
          public var predictorMetadata:
            GenerateGroundedContentResponse.Candidate.GroundingMetadata
              .DynamicRetrievalPredictorMetadata? = nil

          /// Initialize a new instance of `DynamicRetrievalMetadata`.
          public init() {}

          /// Use `config` to return a new instance of this object, with some fields updated.
          ///
          /// Commonly used to initialize the value, for example:
          ///
          /// ```
          /// let value = DynamicRetrievalMetadata().with { $0.predictorMetadata = ... }
          /// ```
          public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
            var copy = self
            try config(&copy)
            return copy
          }

          public static var _anyTypeUrl: Swift.String {
            return
              "type.googleapis.com/google.cloud.discoveryengine.v1.GenerateGroundedContentResponse.Candidate.GroundingMetadata.DynamicRetrievalMetadata"
          }
          public init(fromAny any: GoogleCloudWkt.`Any`) throws {
            self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
          }
          public func _pack() throws -> GoogleCloudWkt.Struct {
            return try GoogleCloudWkt._slowAnySerialize(message: self)
          }
        }

        /// Describes the metadata about the dynamic retrieval predictor.
        public struct DynamicRetrievalPredictorMetadata: Codable, Equatable, GoogleCloudWkt
            ._AnyPackable,
          Sendable
        {
          /// The version of the predictor which was used in dynamic retrieval.
          public var version:
            GenerateGroundedContentResponse.Candidate.GroundingMetadata
              .DynamicRetrievalPredictorMetadata.Version = GenerateGroundedContentResponse.Candidate
              .GroundingMetadata.DynamicRetrievalPredictorMetadata.Version()

          /// The value of the predictor. This should be between [0, 1] where
          /// a value of 0 means that the query would not benefit from grounding,
          /// while a value of 1.0 means that the query would benefit the most.
          /// In between values allow to differentiate between different usefulness
          /// scores for grounding.
          public var prediction: Swift.Float? = nil

          /// Initialize a new instance of `DynamicRetrievalPredictorMetadata`.
          public init() {}

          /// Use `config` to return a new instance of this object, with some fields updated.
          ///
          /// Commonly used to initialize the value, for example:
          ///
          /// ```
          /// let value = DynamicRetrievalPredictorMetadata().with { $0.version = ... }
          /// ```
          public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
            var copy = self
            try config(&copy)
            return copy
          }

          /// The version of the predictor which was used in dynamic retrieval.
          public enum Version: Codable, Equatable, Sendable {
            /// Unspecified version, should never be used.
            case unspecified
            /// The V1 model which is evaluating each source independently.
            case v1Independent
            /// Encodes an unknown integer value.
            ///
            /// The most common cause for an unknown values is for the service to send
            /// a value unknown to the library. We recommend you update your library to
            /// the latest version.
            case unknownIntValue(Int)
            /// Encodes an unknown string value.
            ///
            /// The most common cause for an unknown values is for the service to send
            /// a value unknown to the library. We recommend you update your library to
            /// the latest version.
            case unknownStringValue(String)

            public init() {
              self = .unspecified
            }

            /// Returns the integer value associated with the enumeration.
            ///
            /// If the enumeration was initialized with an unknown string value, this returns `nil`.
            public var intValue: Int? {
              switch self {
              case .unspecified: return 0
              case .v1Independent: return 1
              case .unknownIntValue(let v): return v
              case .unknownStringValue: return nil
              }
            }

            /// Returns the string value (or name) associated with the enumeration.
            ///
            /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
            public var stringValue: Swift.String? {
              switch self {
              case .unspecified: return "VERSION_UNSPECIFIED"
              case .v1Independent: return "V1_INDEPENDENT"
              case .unknownIntValue: return nil
              case .unknownStringValue(let v): return v
              }
            }

            /// Initialize from a string value.
            ///
            /// If the value is unknown, this initializes to [`unknownStringValue`](doc:Version/unknownStringValue(_:)).
            public init(stringValue: Swift.String) {
              switch stringValue {
              case "VERSION_UNSPECIFIED": self = .unspecified
              case "V1_INDEPENDENT": self = .v1Independent
              default: self = .unknownStringValue(stringValue)
              }
            }

            /// Initialize from an integer value.
            ///
            /// If the value is unknown, this initializes to [`unknownIntValue`](doc:Version/unknownIntValue(_:)).
            public init(intValue: Int) {
              switch intValue {
              case 0: self = .unspecified
              case 1: self = .v1Independent
              default: self = .unknownIntValue(intValue)
              }
            }

            public init(from decoder: Decoder) throws {
              let container = try decoder.singleValueContainer()
              if let v = try? container.decode(Int.self) {
                self.init(intValue: v)
                return
              }
              if let s = try? container.decode(String.self) {
                if let v = Int(s) {
                  self.init(intValue: v)
                } else {
                  self.init(stringValue: s)
                }
                return
              }
              throw DecodingError.dataCorruptedError(
                in: container, debugDescription: "Expected enum value, must be integer or string.")
            }

            public func encode(to encoder: Encoder) throws {
              var container = encoder.singleValueContainer()
              switch self {
              case .unspecified: return try container.encode(0)
              case .v1Independent: return try container.encode(1)
              case .unknownIntValue(let v): return try container.encode(v)
              case .unknownStringValue(let v): return try container.encode(v)
              }
            }
          }

          public static var _anyTypeUrl: Swift.String {
            return
              "type.googleapis.com/google.cloud.discoveryengine.v1.GenerateGroundedContentResponse.Candidate.GroundingMetadata.DynamicRetrievalPredictorMetadata"
          }
          public init(fromAny any: GoogleCloudWkt.`Any`) throws {
            self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
          }
          public func _pack() throws -> GoogleCloudWkt.Struct {
            return try GoogleCloudWkt._slowAnySerialize(message: self)
          }
        }

        /// Google search entry point.
        public struct SearchEntryPoint: Codable, Equatable, GoogleCloudWkt._AnyPackable,
          Sendable
        {
          /// Web content snippet that can be embedded in a web page or an app
          /// webview.
          public var renderedContent: Swift.String = Swift.String()

          /// Base64 encoded JSON representing array of <search term, search url>
          /// tuple.
          public var sdkBlob: Foundation.Data = Foundation.Data()

          /// Initialize a new instance of `SearchEntryPoint`.
          public init() {}

          /// Use `config` to return a new instance of this object, with some fields updated.
          ///
          /// Commonly used to initialize the value, for example:
          ///
          /// ```
          /// let value = SearchEntryPoint().with { $0.renderedContent = ... }
          /// ```
          public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
            var copy = self
            try config(&copy)
            return copy
          }

          public static var _anyTypeUrl: Swift.String {
            return
              "type.googleapis.com/google.cloud.discoveryengine.v1.GenerateGroundedContentResponse.Candidate.GroundingMetadata.SearchEntryPoint"
          }
          public init(fromAny any: GoogleCloudWkt.`Any`) throws {
            self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
          }
          public func _pack() throws -> GoogleCloudWkt.Struct {
            return try GoogleCloudWkt._slowAnySerialize(message: self)
          }
        }

        /// Grounding info for a claim in the candidate and its support.
        public struct GroundingSupport: Codable, Equatable, GoogleCloudWkt._AnyPackable,
          Sendable
        {
          /// Text for the claim in the candidate. Always provided when a
          /// support is found.
          public var claimText: Swift.String = Swift.String()

          /// A list of indices (into 'support_chunks') specifying the
          /// citations associated with the claim. For instance [1,3,4] means
          /// that support_chunks[1], support_chunks[3],
          /// support_chunks[4] are the chunks attributed to the claim.
          public var supportChunkIndices: [Swift.Int32] = []

          /// A score in the range of [0, 1] describing how grounded is a specific
          /// claim in the support chunks indicated.
          /// Higher value means that the claim is better supported by the chunks.
          public var supportScore: Swift.Float? = nil

          /// Initialize a new instance of `GroundingSupport`.
          public init() {}

          /// Use `config` to return a new instance of this object, with some fields updated.
          ///
          /// Commonly used to initialize the value, for example:
          ///
          /// ```
          /// let value = GroundingSupport().with { $0.claimText = ... }
          /// ```
          public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
            var copy = self
            try config(&copy)
            return copy
          }

          public static var _anyTypeUrl: Swift.String {
            return
              "type.googleapis.com/google.cloud.discoveryengine.v1.GenerateGroundedContentResponse.Candidate.GroundingMetadata.GroundingSupport"
          }
          public init(fromAny any: GoogleCloudWkt.`Any`) throws {
            self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
          }
          public func _pack() throws -> GoogleCloudWkt.Struct {
            return try GoogleCloudWkt._slowAnySerialize(message: self)
          }
        }

        /// Metadata about an image from the web search.
        public struct ImageMetadata: Codable, Equatable, GoogleCloudWkt._AnyPackable,
          Sendable
        {
          /// Metadata about the full size image.
          public var image:
            GenerateGroundedContentResponse.Candidate.GroundingMetadata.ImageMetadata.Image? = nil

          /// Metadata about the thumbnail.
          public var thumbnail:
            GenerateGroundedContentResponse.Candidate.GroundingMetadata.ImageMetadata.Image? = nil

          /// The details about the website that the image is from.
          public var source:
            GenerateGroundedContentResponse.Candidate.GroundingMetadata.ImageMetadata.WebsiteInfo? =
              nil

          /// Initialize a new instance of `ImageMetadata`.
          public init() {}

          /// Use `config` to return a new instance of this object, with some fields updated.
          ///
          /// Commonly used to initialize the value, for example:
          ///
          /// ```
          /// let value = ImageMetadata().with { $0.image = ... }
          /// ```
          public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
            var copy = self
            try config(&copy)
            return copy
          }

          /// Metadata about the website that the image is from.
          public struct WebsiteInfo: Codable, Equatable, GoogleCloudWkt._AnyPackable,
            Sendable
          {
            /// The url of the website.
            public var uri: Swift.String = Swift.String()

            /// The title of the website.
            public var title: Swift.String = Swift.String()

            /// Initialize a new instance of `WebsiteInfo`.
            public init() {}

            /// Use `config` to return a new instance of this object, with some fields updated.
            ///
            /// Commonly used to initialize the value, for example:
            ///
            /// ```
            /// let value = WebsiteInfo().with { $0.uri = ... }
            /// ```
            public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
              var copy = self
              try config(&copy)
              return copy
            }

            public static var _anyTypeUrl: Swift.String {
              return
                "type.googleapis.com/google.cloud.discoveryengine.v1.GenerateGroundedContentResponse.Candidate.GroundingMetadata.ImageMetadata.WebsiteInfo"
            }
            public init(fromAny any: GoogleCloudWkt.`Any`) throws {
              self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
            }
            public func _pack() throws -> GoogleCloudWkt.Struct {
              return try GoogleCloudWkt._slowAnySerialize(message: self)
            }
          }

          /// Metadata about the image.
          public struct Image: Codable, Equatable, GoogleCloudWkt._AnyPackable,
            Sendable
          {
            /// The url of the image.
            public var uri: Swift.String = Swift.String()

            /// The width of the image in pixels.
            public var width: Swift.Int32 = Swift.Int32()

            /// The height of the image in pixels.
            public var height: Swift.Int32 = Swift.Int32()

            /// Initialize a new instance of `Image`.
            public init() {}

            /// Use `config` to return a new instance of this object, with some fields updated.
            ///
            /// Commonly used to initialize the value, for example:
            ///
            /// ```
            /// let value = Image().with { $0.uri = ... }
            /// ```
            public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
              var copy = self
              try config(&copy)
              return copy
            }

            public static var _anyTypeUrl: Swift.String {
              return
                "type.googleapis.com/google.cloud.discoveryengine.v1.GenerateGroundedContentResponse.Candidate.GroundingMetadata.ImageMetadata.Image"
            }
            public init(fromAny any: GoogleCloudWkt.`Any`) throws {
              self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
            }
            public func _pack() throws -> GoogleCloudWkt.Struct {
              return try GoogleCloudWkt._slowAnySerialize(message: self)
            }
          }

          public static var _anyTypeUrl: Swift.String {
            return
              "type.googleapis.com/google.cloud.discoveryengine.v1.GenerateGroundedContentResponse.Candidate.GroundingMetadata.ImageMetadata"
          }
          public init(fromAny any: GoogleCloudWkt.`Any`) throws {
            self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
          }
          public func _pack() throws -> GoogleCloudWkt.Struct {
            return try GoogleCloudWkt._slowAnySerialize(message: self)
          }
        }

        public static var _anyTypeUrl: Swift.String {
          return
            "type.googleapis.com/google.cloud.discoveryengine.v1.GenerateGroundedContentResponse.Candidate.GroundingMetadata"
        }
        public init(fromAny any: GoogleCloudWkt.`Any`) throws {
          self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWkt.Struct {
          return try GoogleCloudWkt._slowAnySerialize(message: self)
        }
      }

      public static var _anyTypeUrl: Swift.String {
        return
          "type.googleapis.com/google.cloud.discoveryengine.v1.GenerateGroundedContentResponse.Candidate"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.discoveryengine.v1.GenerateGroundedContentResponse"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }
#endif
