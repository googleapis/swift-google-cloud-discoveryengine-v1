// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

#if ConversationalSearchService || SessionService
  import Foundation
  @_spi(GoogleCloudInternal) import GoogleCloudWkt

  /// Defines an answer.
  public struct Answer: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Immutable. Fully qualified name
    /// `projects/{project}/locations/global/collections/{collection}/engines/{engine}/sessions/*/answers/*`
    public var name: Swift.String = Swift.String()

    /// The state of the answer generation.
    public var state: Answer.State = Answer.State()

    /// The textual answer.
    public var answerText: Swift.String = Swift.String()

    /// A score in the range of [0, 1] describing how grounded the answer is by the
    /// reference chunks.
    public var groundingScore: Swift.Double? = nil

    /// Citations.
    public var citations: [Answer.Citation] = []

    /// Optional. Grounding supports.
    public var groundingSupports: [Answer.GroundingSupport] = []

    /// References.
    public var references: [Answer.Reference] = []

    /// Suggested related questions.
    public var relatedQuestions: [Swift.String] = []

    /// Answer generation steps.
    public var steps: [Answer.Step] = []

    /// Query understanding information.
    public var queryUnderstandingInfo: Answer.QueryUnderstandingInfo? = nil

    /// Additional answer-skipped reasons. This provides the reason for ignored
    /// cases. If nothing is skipped, this field is not set.
    public var answerSkippedReasons: [Answer.AnswerSkippedReason] = []

    /// Output only. Answer creation timestamp.
    public var createTime: GoogleCloudWkt.Timestamp? = nil

    /// Output only. Answer completed timestamp.
    public var completeTime: GoogleCloudWkt.Timestamp? = nil

    /// Optional. Safety ratings.
    public var safetyRatings: [SafetyRating] = []

    /// Initialize a new instance of `Answer`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = Answer().with { $0.name = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// Citation info for a segment.
    public struct Citation: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// Index indicates the start of the segment, measured in bytes (UTF-8
      /// unicode). If there are multi-byte characters,such as non-ASCII
      /// characters, the index measurement is longer than the string length.
      public var startIndex: Swift.Int64 = Swift.Int64()

      /// End of the attributed segment, exclusive. Measured in bytes (UTF-8
      /// unicode). If there are multi-byte characters,such as non-ASCII
      /// characters, the index measurement is longer than the string length.
      public var endIndex: Swift.Int64 = Swift.Int64()

      /// Citation sources for the attributed segment.
      public var sources: [Answer.CitationSource] = []

      /// Initialize a new instance of `Citation`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = Citation().with { $0.startIndex = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.cloud.discoveryengine.v1.Answer.Citation"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    /// Citation source.
    public struct CitationSource: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// ID of the citation source.
      public var referenceId: Swift.String = Swift.String()

      /// Initialize a new instance of `CitationSource`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = CitationSource().with { $0.referenceId = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.cloud.discoveryengine.v1.Answer.CitationSource"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    /// Grounding support for a claim in `answer_text`.
    public struct GroundingSupport: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// Required. Index indicates the start of the claim, measured in bytes
      /// (UTF-8 unicode).
      public var startIndex: Swift.Int64 = Swift.Int64()

      /// Required. End of the claim, exclusive.
      public var endIndex: Swift.Int64 = Swift.Int64()

      /// A score in the range of [0, 1] describing how grounded is a specific
      /// claim by the references.
      /// Higher value means that the claim is better supported by the reference
      /// chunks.
      public var groundingScore: Swift.Double? = nil

      /// Indicates that this claim required grounding check. When the
      /// system decided this claim didn't require attribution/grounding check,
      /// this field is set to false. In that case, no grounding check was
      /// done for the claim and therefore `grounding_score`, `sources` is not
      /// returned.
      public var groundingCheckRequired: Swift.Bool? = nil

      /// Optional. Citation sources for the claim.
      public var sources: [Answer.CitationSource] = []

      /// Initialize a new instance of `GroundingSupport`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = GroundingSupport().with { $0.startIndex = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.cloud.discoveryengine.v1.Answer.GroundingSupport"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    /// Reference.
    public struct Reference: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// Search result content.
      public var content: OneOf_Content? = nil

      /// Initialize a new instance of `Reference`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = Reference().with { $0.unstructuredDocumentInfo = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      private enum CodingKeys: Swift.String, CodingKey {
        case unstructuredDocumentInfo = "unstructuredDocumentInfo"
        case chunkInfo = "chunkInfo"
        case structuredDocumentInfo = "structuredDocumentInfo"
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        var content: OneOf_Content? = nil
        let contentCheckAndSet = {
          if content != nil {
            throw DecodingError.dataCorrupted(
              DecodingError.Context(
                codingPath: decoder.codingPath,
                debugDescription: "Multiple values set for oneof 'content'"))
          }
          content = $0
        }
        if let unstructuredDocumentInfo = try container.decodeIfPresent(
          Answer.Reference.UnstructuredDocumentInfo?.self, forKey: .unstructuredDocumentInfo)
        {
          try contentCheckAndSet(.unstructuredDocumentInfo(unstructuredDocumentInfo))
        }
        if let chunkInfo = try container.decodeIfPresent(
          Answer.Reference.ChunkInfo?.self, forKey: .chunkInfo)
        {
          try contentCheckAndSet(.chunkInfo(chunkInfo))
        }
        if let structuredDocumentInfo = try container.decodeIfPresent(
          Answer.Reference.StructuredDocumentInfo?.self, forKey: .structuredDocumentInfo)
        {
          try contentCheckAndSet(.structuredDocumentInfo(structuredDocumentInfo))
        }
        self.content = content
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        if let choice = self.content {
          switch choice {
          case .unstructuredDocumentInfo(let value):
            try container.encode(value, forKey: .unstructuredDocumentInfo)
          case .chunkInfo(let value):
            try container.encode(value, forKey: .chunkInfo)
          case .structuredDocumentInfo(let value):
            try container.encode(value, forKey: .structuredDocumentInfo)
          }
        }
      }

      /// Unstructured document information.
      public struct UnstructuredDocumentInfo: Codable, Equatable, GoogleCloudWkt._AnyPackable,
        Sendable
      {
        /// Document resource name.
        public var document: Swift.String = Swift.String()

        /// URI for the document.
        public var uri: Swift.String = Swift.String()

        /// Title.
        public var title: Swift.String = Swift.String()

        /// List of cited chunk contents derived from document content.
        public var chunkContents: [Answer.Reference.UnstructuredDocumentInfo.ChunkContent] = []

        /// The structured JSON metadata for the document.
        /// It is populated from the struct data from the Chunk in search result.
        public var structData: GoogleCloudWkt.Struct? = nil

        /// Initialize a new instance of `UnstructuredDocumentInfo`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = UnstructuredDocumentInfo().with { $0.document = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        /// Chunk content.
        public struct ChunkContent: Codable, Equatable, GoogleCloudWkt._AnyPackable,
          Sendable
        {
          /// Chunk textual content.
          public var content: Swift.String = Swift.String()

          /// Page identifier.
          public var pageIdentifier: Swift.String = Swift.String()

          /// The relevance of the chunk for a given query. Values range from 0.0
          /// (completely irrelevant) to 1.0 (completely relevant).
          /// This value is for informational purpose only. It may change for
          /// the same query and chunk at any time due to a model retraining or
          /// change in implementation.
          public var relevanceScore: Swift.Float? = nil

          /// Initialize a new instance of `ChunkContent`.
          public init() {}

          /// Use `config` to return a new instance of this object, with some fields updated.
          ///
          /// Commonly used to initialize the value, for example:
          ///
          /// ```
          /// let value = ChunkContent().with { $0.content = ... }
          /// ```
          public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
            var copy = self
            try config(&copy)
            return copy
          }

          public static var _anyTypeUrl: Swift.String {
            return
              "type.googleapis.com/google.cloud.discoveryengine.v1.Answer.Reference.UnstructuredDocumentInfo.ChunkContent"
          }
          public init(fromAny any: GoogleCloudWkt.`Any`) throws {
            self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
          }
          public func _pack() throws -> GoogleCloudWkt.Struct {
            return try GoogleCloudWkt._slowAnySerialize(message: self)
          }
        }

        public static var _anyTypeUrl: Swift.String {
          return
            "type.googleapis.com/google.cloud.discoveryengine.v1.Answer.Reference.UnstructuredDocumentInfo"
        }
        public init(fromAny any: GoogleCloudWkt.`Any`) throws {
          self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWkt.Struct {
          return try GoogleCloudWkt._slowAnySerialize(message: self)
        }
      }

      /// Chunk information.
      public struct ChunkInfo: Codable, Equatable, GoogleCloudWkt._AnyPackable,
        Sendable
      {
        /// Chunk resource name.
        public var chunk: Swift.String = Swift.String()

        /// Chunk textual content.
        public var content: Swift.String = Swift.String()

        /// The relevance of the chunk for a given query. Values range from 0.0
        /// (completely irrelevant) to 1.0 (completely relevant).
        /// This value is for informational purpose only. It may change for
        /// the same query and chunk at any time due to a model retraining or
        /// change in implementation.
        public var relevanceScore: Swift.Float? = nil

        /// Document metadata.
        public var documentMetadata: Answer.Reference.ChunkInfo.DocumentMetadata? = nil

        /// Initialize a new instance of `ChunkInfo`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = ChunkInfo().with { $0.chunk = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        /// Document metadata.
        public struct DocumentMetadata: Codable, Equatable, GoogleCloudWkt._AnyPackable,
          Sendable
        {
          /// Document resource name.
          public var document: Swift.String = Swift.String()

          /// URI for the document.
          public var uri: Swift.String = Swift.String()

          /// Title.
          public var title: Swift.String = Swift.String()

          /// Page identifier.
          public var pageIdentifier: Swift.String = Swift.String()

          /// The structured JSON metadata for the document.
          /// It is populated from the struct data from the Chunk in search result.
          public var structData: GoogleCloudWkt.Struct? = nil

          /// Initialize a new instance of `DocumentMetadata`.
          public init() {}

          /// Use `config` to return a new instance of this object, with some fields updated.
          ///
          /// Commonly used to initialize the value, for example:
          ///
          /// ```
          /// let value = DocumentMetadata().with { $0.document = ... }
          /// ```
          public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
            var copy = self
            try config(&copy)
            return copy
          }

          public static var _anyTypeUrl: Swift.String {
            return
              "type.googleapis.com/google.cloud.discoveryengine.v1.Answer.Reference.ChunkInfo.DocumentMetadata"
          }
          public init(fromAny any: GoogleCloudWkt.`Any`) throws {
            self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
          }
          public func _pack() throws -> GoogleCloudWkt.Struct {
            return try GoogleCloudWkt._slowAnySerialize(message: self)
          }
        }

        public static var _anyTypeUrl: Swift.String {
          return "type.googleapis.com/google.cloud.discoveryengine.v1.Answer.Reference.ChunkInfo"
        }
        public init(fromAny any: GoogleCloudWkt.`Any`) throws {
          self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWkt.Struct {
          return try GoogleCloudWkt._slowAnySerialize(message: self)
        }
      }

      /// Structured search information.
      public struct StructuredDocumentInfo: Codable, Equatable, GoogleCloudWkt._AnyPackable,
        Sendable
      {
        /// Document resource name.
        public var document: Swift.String = Swift.String()

        /// Structured search data.
        public var structData: GoogleCloudWkt.Struct? = nil

        /// Output only. The title of the document.
        public var title: Swift.String = Swift.String()

        /// Output only. The URI of the document.
        public var uri: Swift.String = Swift.String()

        /// Initialize a new instance of `StructuredDocumentInfo`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = StructuredDocumentInfo().with { $0.document = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        public static var _anyTypeUrl: Swift.String {
          return
            "type.googleapis.com/google.cloud.discoveryengine.v1.Answer.Reference.StructuredDocumentInfo"
        }
        public init(fromAny any: GoogleCloudWkt.`Any`) throws {
          self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWkt.Struct {
          return try GoogleCloudWkt._slowAnySerialize(message: self)
        }
      }

      /// Search result content.
      public enum OneOf_Content: Codable, Equatable, Sendable {
        /// Unstructured document information.
        indirect case unstructuredDocumentInfo(Answer.Reference.UnstructuredDocumentInfo?)
        /// Chunk information.
        indirect case chunkInfo(Answer.Reference.ChunkInfo?)
        /// Structured document information.
        indirect case structuredDocumentInfo(Answer.Reference.StructuredDocumentInfo?)
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.cloud.discoveryengine.v1.Answer.Reference"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    /// Step information.
    public struct Step: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// The state of the step.
      public var state: Answer.Step.State = Answer.Step.State()

      /// The description of the step.
      public var description: Swift.String = Swift.String()

      /// The thought of the step.
      public var thought: Swift.String = Swift.String()

      /// Actions.
      public var actions: [Answer.Step.Action] = []

      /// Initialize a new instance of `Step`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = Step().with { $0.state = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      /// Action.
      public struct Action: Codable, Equatable, GoogleCloudWkt._AnyPackable,
        Sendable
      {
        /// Observation.
        public var observation: Answer.Step.Action.Observation? = nil

        /// The action.
        public var action: OneOf_Action? = nil

        /// Initialize a new instance of `Action`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = Action().with { $0.searchAction = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        private enum CodingKeys: Swift.String, CodingKey {
          case searchAction = "searchAction"
          case observation = "observation"
        }

        public init(from decoder: Decoder) throws {
          let container = try decoder.container(keyedBy: CodingKeys.self)
          self.observation = try container.decodeIfPresent(
            Answer.Step.Action.Observation.self, forKey: .observation)

          var action: OneOf_Action? = nil
          let actionCheckAndSet = {
            if action != nil {
              throw DecodingError.dataCorrupted(
                DecodingError.Context(
                  codingPath: decoder.codingPath,
                  debugDescription: "Multiple values set for oneof 'action'"))
            }
            action = $0
          }
          if let searchAction = try container.decodeIfPresent(
            Answer.Step.Action.SearchAction?.self, forKey: .searchAction)
          {
            try actionCheckAndSet(.searchAction(searchAction))
          }
          self.action = action
        }

        public func encode(to encoder: Encoder) throws {
          var container = encoder.container(keyedBy: CodingKeys.self)
          try container.encode(self.observation, forKey: .observation)

          if let choice = self.action {
            switch choice {
            case .searchAction(let value):
              try container.encode(value, forKey: .searchAction)
            }
          }
        }

        /// Search action.
        public struct SearchAction: Codable, Equatable, GoogleCloudWkt._AnyPackable,
          Sendable
        {
          /// The query to search.
          public var query: Swift.String = Swift.String()

          /// Initialize a new instance of `SearchAction`.
          public init() {}

          /// Use `config` to return a new instance of this object, with some fields updated.
          ///
          /// Commonly used to initialize the value, for example:
          ///
          /// ```
          /// let value = SearchAction().with { $0.query = ... }
          /// ```
          public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
            var copy = self
            try config(&copy)
            return copy
          }

          public static var _anyTypeUrl: Swift.String {
            return
              "type.googleapis.com/google.cloud.discoveryengine.v1.Answer.Step.Action.SearchAction"
          }
          public init(fromAny any: GoogleCloudWkt.`Any`) throws {
            self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
          }
          public func _pack() throws -> GoogleCloudWkt.Struct {
            return try GoogleCloudWkt._slowAnySerialize(message: self)
          }
        }

        /// Observation.
        public struct Observation: Codable, Equatable, GoogleCloudWkt._AnyPackable,
          Sendable
        {
          /// Search results observed by the search action, it can be snippets info
          /// or chunk info, depending on the citation type set by the user.
          public var searchResults: [Answer.Step.Action.Observation.SearchResult] = []

          /// Initialize a new instance of `Observation`.
          public init() {}

          /// Use `config` to return a new instance of this object, with some fields updated.
          ///
          /// Commonly used to initialize the value, for example:
          ///
          /// ```
          /// let value = Observation().with { $0.searchResults = ... }
          /// ```
          public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
            var copy = self
            try config(&copy)
            return copy
          }

          public struct SearchResult: Codable, Equatable, GoogleCloudWkt._AnyPackable,
            Sendable
          {
            /// Document resource name.
            public var document: Swift.String = Swift.String()

            /// URI for the document.
            public var uri: Swift.String = Swift.String()

            /// Title.
            public var title: Swift.String = Swift.String()

            /// If citation_type is DOCUMENT_LEVEL_CITATION, populate document
            /// level snippets.
            public var snippetInfo: [Answer.Step.Action.Observation.SearchResult.SnippetInfo] = []

            /// If citation_type is CHUNK_LEVEL_CITATION and chunk mode is on,
            /// populate chunk info.
            public var chunkInfo: [Answer.Step.Action.Observation.SearchResult.ChunkInfo] = []

            /// Data representation.
            /// The structured JSON data for the document.
            /// It's populated from the struct data from the Document, or the
            /// Chunk in search result.
            public var structData: GoogleCloudWkt.Struct? = nil

            /// Initialize a new instance of `SearchResult`.
            public init() {}

            /// Use `config` to return a new instance of this object, with some fields updated.
            ///
            /// Commonly used to initialize the value, for example:
            ///
            /// ```
            /// let value = SearchResult().with { $0.document = ... }
            /// ```
            public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
              var copy = self
              try config(&copy)
              return copy
            }

            /// Snippet information.
            public struct SnippetInfo: Codable, Equatable, GoogleCloudWkt._AnyPackable,
              Sendable
            {
              /// Snippet content.
              public var snippet: Swift.String = Swift.String()

              /// Status of the snippet defined by the search team.
              public var snippetStatus: Swift.String = Swift.String()

              /// Initialize a new instance of `SnippetInfo`.
              public init() {}

              /// Use `config` to return a new instance of this object, with some fields updated.
              ///
              /// Commonly used to initialize the value, for example:
              ///
              /// ```
              /// let value = SnippetInfo().with { $0.snippet = ... }
              /// ```
              public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
                var copy = self
                try config(&copy)
                return copy
              }

              public static var _anyTypeUrl: Swift.String {
                return
                  "type.googleapis.com/google.cloud.discoveryengine.v1.Answer.Step.Action.Observation.SearchResult.SnippetInfo"
              }
              public init(fromAny any: GoogleCloudWkt.`Any`) throws {
                self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
              }
              public func _pack() throws -> GoogleCloudWkt.Struct {
                return try GoogleCloudWkt._slowAnySerialize(message: self)
              }
            }

            /// Chunk information.
            public struct ChunkInfo: Codable, Equatable, GoogleCloudWkt._AnyPackable,
              Sendable
            {
              /// Chunk resource name.
              public var chunk: Swift.String = Swift.String()

              /// Chunk textual content.
              public var content: Swift.String = Swift.String()

              /// The relevance of the chunk for a given query. Values range from
              /// 0.0 (completely irrelevant) to 1.0 (completely relevant).
              /// This value is for informational purpose only. It may change for
              /// the same query and chunk at any time due to a model retraining or
              /// change in implementation.
              public var relevanceScore: Swift.Float? = nil

              /// Initialize a new instance of `ChunkInfo`.
              public init() {}

              /// Use `config` to return a new instance of this object, with some fields updated.
              ///
              /// Commonly used to initialize the value, for example:
              ///
              /// ```
              /// let value = ChunkInfo().with { $0.chunk = ... }
              /// ```
              public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
                var copy = self
                try config(&copy)
                return copy
              }

              public static var _anyTypeUrl: Swift.String {
                return
                  "type.googleapis.com/google.cloud.discoveryengine.v1.Answer.Step.Action.Observation.SearchResult.ChunkInfo"
              }
              public init(fromAny any: GoogleCloudWkt.`Any`) throws {
                self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
              }
              public func _pack() throws -> GoogleCloudWkt.Struct {
                return try GoogleCloudWkt._slowAnySerialize(message: self)
              }
            }

            public static var _anyTypeUrl: Swift.String {
              return
                "type.googleapis.com/google.cloud.discoveryengine.v1.Answer.Step.Action.Observation.SearchResult"
            }
            public init(fromAny any: GoogleCloudWkt.`Any`) throws {
              self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
            }
            public func _pack() throws -> GoogleCloudWkt.Struct {
              return try GoogleCloudWkt._slowAnySerialize(message: self)
            }
          }

          public static var _anyTypeUrl: Swift.String {
            return
              "type.googleapis.com/google.cloud.discoveryengine.v1.Answer.Step.Action.Observation"
          }
          public init(fromAny any: GoogleCloudWkt.`Any`) throws {
            self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
          }
          public func _pack() throws -> GoogleCloudWkt.Struct {
            return try GoogleCloudWkt._slowAnySerialize(message: self)
          }
        }

        /// The action.
        public enum OneOf_Action: Codable, Equatable, Sendable {
          /// Search action.
          indirect case searchAction(Answer.Step.Action.SearchAction?)
        }

        public static var _anyTypeUrl: Swift.String {
          return "type.googleapis.com/google.cloud.discoveryengine.v1.Answer.Step.Action"
        }
        public init(fromAny any: GoogleCloudWkt.`Any`) throws {
          self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWkt.Struct {
          return try GoogleCloudWkt._slowAnySerialize(message: self)
        }
      }

      /// Enumeration of the state of the step.
      public enum State: Codable, Equatable, Sendable {
        /// Unknown.
        case unspecified
        /// Step is currently in progress.
        case inProgress
        /// Step currently failed.
        case failed
        /// Step has succeeded.
        case succeeded
        /// Encodes an unknown integer value.
        ///
        /// The most common cause for an unknown values is for the service to send
        /// a value unknown to the library. We recommend you update your library to
        /// the latest version.
        case unknownIntValue(Int)
        /// Encodes an unknown string value.
        ///
        /// The most common cause for an unknown values is for the service to send
        /// a value unknown to the library. We recommend you update your library to
        /// the latest version.
        case unknownStringValue(String)

        public init() {
          self = .unspecified
        }

        /// Returns the integer value associated with the enumeration.
        ///
        /// If the enumeration was initialized with an unknown string value, this returns `nil`.
        public var intValue: Int? {
          switch self {
          case .unspecified: return 0
          case .inProgress: return 1
          case .failed: return 2
          case .succeeded: return 3
          case .unknownIntValue(let v): return v
          case .unknownStringValue: return nil
          }
        }

        /// Returns the string value (or name) associated with the enumeration.
        ///
        /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
        public var stringValue: Swift.String? {
          switch self {
          case .unspecified: return "STATE_UNSPECIFIED"
          case .inProgress: return "IN_PROGRESS"
          case .failed: return "FAILED"
          case .succeeded: return "SUCCEEDED"
          case .unknownIntValue: return nil
          case .unknownStringValue(let v): return v
          }
        }

        /// Initialize from a string value.
        ///
        /// If the value is unknown, this initializes to [`unknownStringValue`](doc:State/unknownStringValue(_:)).
        public init(stringValue: Swift.String) {
          switch stringValue {
          case "STATE_UNSPECIFIED": self = .unspecified
          case "IN_PROGRESS": self = .inProgress
          case "FAILED": self = .failed
          case "SUCCEEDED": self = .succeeded
          default: self = .unknownStringValue(stringValue)
          }
        }

        /// Initialize from an integer value.
        ///
        /// If the value is unknown, this initializes to [`unknownIntValue`](doc:State/unknownIntValue(_:)).
        public init(intValue: Int) {
          switch intValue {
          case 0: self = .unspecified
          case 1: self = .inProgress
          case 2: self = .failed
          case 3: self = .succeeded
          default: self = .unknownIntValue(intValue)
          }
        }

        public init(from decoder: Decoder) throws {
          let container = try decoder.singleValueContainer()
          if let v = try? container.decode(Int.self) {
            self.init(intValue: v)
            return
          }
          if let s = try? container.decode(String.self) {
            if let v = Int(s) {
              self.init(intValue: v)
            } else {
              self.init(stringValue: s)
            }
            return
          }
          throw DecodingError.dataCorruptedError(
            in: container, debugDescription: "Expected enum value, must be integer or string.")
        }

        public func encode(to encoder: Encoder) throws {
          var container = encoder.singleValueContainer()
          switch self {
          case .unspecified: return try container.encode(0)
          case .inProgress: return try container.encode(1)
          case .failed: return try container.encode(2)
          case .succeeded: return try container.encode(3)
          case .unknownIntValue(let v): return try container.encode(v)
          case .unknownStringValue(let v): return try container.encode(v)
          }
        }
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.cloud.discoveryengine.v1.Answer.Step"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    /// Query understanding information.
    public struct QueryUnderstandingInfo: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// Query classification information.
      public var queryClassificationInfo: [Answer.QueryUnderstandingInfo.QueryClassificationInfo] =
        []

      /// Initialize a new instance of `QueryUnderstandingInfo`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = QueryUnderstandingInfo().with { $0.queryClassificationInfo = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      /// Query classification information.
      public struct QueryClassificationInfo: Codable, Equatable, GoogleCloudWkt._AnyPackable,
        Sendable
      {
        /// Query classification type.
        public var type: Answer.QueryUnderstandingInfo.QueryClassificationInfo.Type_ = Answer
          .QueryUnderstandingInfo.QueryClassificationInfo.Type_()

        /// Classification output.
        public var positive: Swift.Bool = Swift.Bool()

        /// Initialize a new instance of `QueryClassificationInfo`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = QueryClassificationInfo().with { $0.type = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        /// Query classification types.
        public enum Type_: Codable, Equatable, Sendable {
          /// Unspecified query classification type.
          case unspecified
          /// Adversarial query classification type.
          case adversarialQuery
          /// Non-answer-seeking query classification type, for chit chat.
          case nonAnswerSeekingQuery
          /// Jail-breaking query classification type.
          case jailBreakingQuery
          /// Non-answer-seeking query classification type, for no clear intent.
          case nonAnswerSeekingQueryV2
          /// User defined query classification type.
          case userDefinedClassificationQuery
          /// Encodes an unknown integer value.
          ///
          /// The most common cause for an unknown values is for the service to send
          /// a value unknown to the library. We recommend you update your library to
          /// the latest version.
          case unknownIntValue(Int)
          /// Encodes an unknown string value.
          ///
          /// The most common cause for an unknown values is for the service to send
          /// a value unknown to the library. We recommend you update your library to
          /// the latest version.
          case unknownStringValue(String)

          public init() {
            self = .unspecified
          }

          /// Returns the integer value associated with the enumeration.
          ///
          /// If the enumeration was initialized with an unknown string value, this returns `nil`.
          public var intValue: Int? {
            switch self {
            case .unspecified: return 0
            case .adversarialQuery: return 1
            case .nonAnswerSeekingQuery: return 2
            case .jailBreakingQuery: return 3
            case .nonAnswerSeekingQueryV2: return 4
            case .userDefinedClassificationQuery: return 5
            case .unknownIntValue(let v): return v
            case .unknownStringValue: return nil
            }
          }

          /// Returns the string value (or name) associated with the enumeration.
          ///
          /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
          public var stringValue: Swift.String? {
            switch self {
            case .unspecified: return "TYPE_UNSPECIFIED"
            case .adversarialQuery: return "ADVERSARIAL_QUERY"
            case .nonAnswerSeekingQuery: return "NON_ANSWER_SEEKING_QUERY"
            case .jailBreakingQuery: return "JAIL_BREAKING_QUERY"
            case .nonAnswerSeekingQueryV2: return "NON_ANSWER_SEEKING_QUERY_V2"
            case .userDefinedClassificationQuery: return "USER_DEFINED_CLASSIFICATION_QUERY"
            case .unknownIntValue: return nil
            case .unknownStringValue(let v): return v
            }
          }

          /// Initialize from a string value.
          ///
          /// If the value is unknown, this initializes to [`unknownStringValue`](doc:Type_/unknownStringValue(_:)).
          public init(stringValue: Swift.String) {
            switch stringValue {
            case "TYPE_UNSPECIFIED": self = .unspecified
            case "ADVERSARIAL_QUERY": self = .adversarialQuery
            case "NON_ANSWER_SEEKING_QUERY": self = .nonAnswerSeekingQuery
            case "JAIL_BREAKING_QUERY": self = .jailBreakingQuery
            case "NON_ANSWER_SEEKING_QUERY_V2": self = .nonAnswerSeekingQueryV2
            case "USER_DEFINED_CLASSIFICATION_QUERY": self = .userDefinedClassificationQuery
            default: self = .unknownStringValue(stringValue)
            }
          }

          /// Initialize from an integer value.
          ///
          /// If the value is unknown, this initializes to [`unknownIntValue`](doc:Type_/unknownIntValue(_:)).
          public init(intValue: Int) {
            switch intValue {
            case 0: self = .unspecified
            case 1: self = .adversarialQuery
            case 2: self = .nonAnswerSeekingQuery
            case 3: self = .jailBreakingQuery
            case 4: self = .nonAnswerSeekingQueryV2
            case 5: self = .userDefinedClassificationQuery
            default: self = .unknownIntValue(intValue)
            }
          }

          public init(from decoder: Decoder) throws {
            let container = try decoder.singleValueContainer()
            if let v = try? container.decode(Int.self) {
              self.init(intValue: v)
              return
            }
            if let s = try? container.decode(String.self) {
              if let v = Int(s) {
                self.init(intValue: v)
              } else {
                self.init(stringValue: s)
              }
              return
            }
            throw DecodingError.dataCorruptedError(
              in: container, debugDescription: "Expected enum value, must be integer or string.")
          }

          public func encode(to encoder: Encoder) throws {
            var container = encoder.singleValueContainer()
            switch self {
            case .unspecified: return try container.encode(0)
            case .adversarialQuery: return try container.encode(1)
            case .nonAnswerSeekingQuery: return try container.encode(2)
            case .jailBreakingQuery: return try container.encode(3)
            case .nonAnswerSeekingQueryV2: return try container.encode(4)
            case .userDefinedClassificationQuery: return try container.encode(5)
            case .unknownIntValue(let v): return try container.encode(v)
            case .unknownStringValue(let v): return try container.encode(v)
            }
          }
        }

        public static var _anyTypeUrl: Swift.String {
          return
            "type.googleapis.com/google.cloud.discoveryengine.v1.Answer.QueryUnderstandingInfo.QueryClassificationInfo"
        }
        public init(fromAny any: GoogleCloudWkt.`Any`) throws {
          self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWkt.Struct {
          return try GoogleCloudWkt._slowAnySerialize(message: self)
        }
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.cloud.discoveryengine.v1.Answer.QueryUnderstandingInfo"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    /// Enumeration of the state of the answer generation.
    public enum State: Codable, Equatable, Sendable {
      /// Unknown.
      case unspecified
      /// Answer generation is currently in progress.
      case inProgress
      /// Answer generation currently failed.
      case failed
      /// Answer generation has succeeded.
      case succeeded
      /// Answer generation is currently in progress.
      case streaming
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .inProgress: return 1
        case .failed: return 2
        case .succeeded: return 3
        case .streaming: return 4
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "STATE_UNSPECIFIED"
        case .inProgress: return "IN_PROGRESS"
        case .failed: return "FAILED"
        case .succeeded: return "SUCCEEDED"
        case .streaming: return "STREAMING"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:State/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "STATE_UNSPECIFIED": self = .unspecified
        case "IN_PROGRESS": self = .inProgress
        case "FAILED": self = .failed
        case "SUCCEEDED": self = .succeeded
        case "STREAMING": self = .streaming
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:State/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .inProgress
        case 2: self = .failed
        case 3: self = .succeeded
        case 4: self = .streaming
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .inProgress: return try container.encode(1)
        case .failed: return try container.encode(2)
        case .succeeded: return try container.encode(3)
        case .streaming: return try container.encode(4)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    /// An enum for answer skipped reasons.
    public enum AnswerSkippedReason: Codable, Equatable, Sendable {
      /// Default value. The answer skipped reason is not specified.
      case unspecified
      /// The adversarial query ignored case.
      case adversarialQueryIgnored
      /// The non-answer seeking query ignored case
      ///
      /// Google skips the answer if the query is chit chat.
      case nonAnswerSeekingQueryIgnored
      /// The out-of-domain query ignored case.
      ///
      /// Google skips the answer if there are no high-relevance search results.
      case outOfDomainQueryIgnored
      /// The potential policy violation case.
      ///
      /// Google skips the answer if there is a potential policy violation
      /// detected. This includes content that may be violent or toxic.
      case potentialPolicyViolation
      /// The no relevant content case.
      ///
      /// Google skips the answer if there is no relevant content in the
      /// retrieved search results.
      case noRelevantContent
      /// The jail-breaking query ignored case.
      ///
      /// For example, "Reply in the tone of a competing company's CEO".
      /// Google skips the answer if the query is classified as a jail-breaking
      /// query.
      case jailBreakingQueryIgnored
      /// The customer policy violation case.
      ///
      /// Google skips the summary if there is a customer policy violation
      /// detected. The policy is defined by the customer.
      case customerPolicyViolation
      /// The non-answer seeking query ignored case.
      ///
      /// Google skips the answer if the query doesn't have clear intent.
      case nonAnswerSeekingQueryIgnoredV2
      /// The low-grounded answer case.
      ///
      /// Google skips the answer if a well grounded answer was unable to be
      /// generated.
      case lowGroundedAnswer
      /// The user defined query classification ignored case.
      ///
      /// Google skips the answer if the query is classified as a user defined
      /// query classification.
      case userDefinedClassificationQueryIgnored
      /// The unhelpful answer case.
      ///
      /// Google skips the answer if the answer is not helpful. This can be due to
      /// a variety of factors, including but not limited to: the query is not
      /// answerable, the answer is not relevant to the query, or the answer is
      /// not well-formatted.
      case unhelpfulAnswer
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .adversarialQueryIgnored: return 1
        case .nonAnswerSeekingQueryIgnored: return 2
        case .outOfDomainQueryIgnored: return 3
        case .potentialPolicyViolation: return 4
        case .noRelevantContent: return 5
        case .jailBreakingQueryIgnored: return 6
        case .customerPolicyViolation: return 7
        case .nonAnswerSeekingQueryIgnoredV2: return 8
        case .lowGroundedAnswer: return 9
        case .userDefinedClassificationQueryIgnored: return 10
        case .unhelpfulAnswer: return 11
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "ANSWER_SKIPPED_REASON_UNSPECIFIED"
        case .adversarialQueryIgnored: return "ADVERSARIAL_QUERY_IGNORED"
        case .nonAnswerSeekingQueryIgnored: return "NON_ANSWER_SEEKING_QUERY_IGNORED"
        case .outOfDomainQueryIgnored: return "OUT_OF_DOMAIN_QUERY_IGNORED"
        case .potentialPolicyViolation: return "POTENTIAL_POLICY_VIOLATION"
        case .noRelevantContent: return "NO_RELEVANT_CONTENT"
        case .jailBreakingQueryIgnored: return "JAIL_BREAKING_QUERY_IGNORED"
        case .customerPolicyViolation: return "CUSTOMER_POLICY_VIOLATION"
        case .nonAnswerSeekingQueryIgnoredV2: return "NON_ANSWER_SEEKING_QUERY_IGNORED_V2"
        case .lowGroundedAnswer: return "LOW_GROUNDED_ANSWER"
        case .userDefinedClassificationQueryIgnored:
          return "USER_DEFINED_CLASSIFICATION_QUERY_IGNORED"
        case .unhelpfulAnswer: return "UNHELPFUL_ANSWER"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:AnswerSkippedReason/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "ANSWER_SKIPPED_REASON_UNSPECIFIED": self = .unspecified
        case "ADVERSARIAL_QUERY_IGNORED": self = .adversarialQueryIgnored
        case "NON_ANSWER_SEEKING_QUERY_IGNORED": self = .nonAnswerSeekingQueryIgnored
        case "OUT_OF_DOMAIN_QUERY_IGNORED": self = .outOfDomainQueryIgnored
        case "POTENTIAL_POLICY_VIOLATION": self = .potentialPolicyViolation
        case "NO_RELEVANT_CONTENT": self = .noRelevantContent
        case "JAIL_BREAKING_QUERY_IGNORED": self = .jailBreakingQueryIgnored
        case "CUSTOMER_POLICY_VIOLATION": self = .customerPolicyViolation
        case "NON_ANSWER_SEEKING_QUERY_IGNORED_V2": self = .nonAnswerSeekingQueryIgnoredV2
        case "LOW_GROUNDED_ANSWER": self = .lowGroundedAnswer
        case "USER_DEFINED_CLASSIFICATION_QUERY_IGNORED":
          self = .userDefinedClassificationQueryIgnored
        case "UNHELPFUL_ANSWER": self = .unhelpfulAnswer
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:AnswerSkippedReason/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .adversarialQueryIgnored
        case 2: self = .nonAnswerSeekingQueryIgnored
        case 3: self = .outOfDomainQueryIgnored
        case 4: self = .potentialPolicyViolation
        case 5: self = .noRelevantContent
        case 6: self = .jailBreakingQueryIgnored
        case 7: self = .customerPolicyViolation
        case 8: self = .nonAnswerSeekingQueryIgnoredV2
        case 9: self = .lowGroundedAnswer
        case 10: self = .userDefinedClassificationQueryIgnored
        case 11: self = .unhelpfulAnswer
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .adversarialQueryIgnored: return try container.encode(1)
        case .nonAnswerSeekingQueryIgnored: return try container.encode(2)
        case .outOfDomainQueryIgnored: return try container.encode(3)
        case .potentialPolicyViolation: return try container.encode(4)
        case .noRelevantContent: return try container.encode(5)
        case .jailBreakingQueryIgnored: return try container.encode(6)
        case .customerPolicyViolation: return try container.encode(7)
        case .nonAnswerSeekingQueryIgnoredV2: return try container.encode(8)
        case .lowGroundedAnswer: return try container.encode(9)
        case .userDefinedClassificationQueryIgnored: return try container.encode(10)
        case .unhelpfulAnswer: return try container.encode(11)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.discoveryengine.v1.Answer"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }
#endif
