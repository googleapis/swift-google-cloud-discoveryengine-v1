// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

#if SiteSearchEngineService
  import Foundation
  @_spi(GoogleCloudInternal) import GoogleCloudWkt

  /// A target site for the SiteSearchEngine.
  public struct TargetSite: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Output only. The fully qualified resource name of the target site.
    /// `projects/{project}/locations/{location}/collections/{collection}/dataStores/{data_store}/siteSearchEngine/targetSites/{target_site}`
    /// The `target_site_id` is system-generated.
    public var name: Swift.String = Swift.String()

    /// Required. Input only. The user provided URI pattern from which the
    /// `generated_uri_pattern` is generated.
    public var providedUriPattern: Swift.String = Swift.String()

    /// The type of the target site, e.g., whether the site is to be included or
    /// excluded.
    public var type: TargetSite.Type_ = TargetSite.Type_()

    /// Immutable. If set to false, a uri_pattern is generated to include all pages
    /// whose address contains the provided_uri_pattern. If set to true, an
    /// uri_pattern is generated to try to be an exact match of the
    /// provided_uri_pattern or just the specific page if the provided_uri_pattern
    /// is a specific one. provided_uri_pattern is always normalized to
    /// generate the URI pattern to be used by the search engine.
    public var exactMatch: Swift.Bool = Swift.Bool()

    /// Output only. This is system-generated based on the provided_uri_pattern.
    public var generatedUriPattern: Swift.String = Swift.String()

    /// Output only. Root domain of the provided_uri_pattern.
    public var rootDomainUri: Swift.String = Swift.String()

    /// Output only. Site ownership and validity verification status.
    public var siteVerificationInfo: SiteVerificationInfo? = nil

    /// Output only. Indexing status.
    public var indexingStatus: TargetSite.IndexingStatus = TargetSite.IndexingStatus()

    /// Output only. The target site's last updated time.
    public var updateTime: GoogleCloudWkt.Timestamp? = nil

    /// Output only. Failure reason.
    public var failureReason: TargetSite.FailureReason? = nil

    /// Initialize a new instance of `TargetSite`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = TargetSite().with { $0.name = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// Site search indexing failure reasons.
    public struct FailureReason: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// Failure reason.
      public var failure: OneOf_Failure? = nil

      /// Initialize a new instance of `FailureReason`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = FailureReason().with { $0.quotaFailure = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      private enum CodingKeys: Swift.String, CodingKey {
        case quotaFailure = "quotaFailure"
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        var failure: OneOf_Failure? = nil
        let failureCheckAndSet = {
          if failure != nil {
            throw DecodingError.dataCorrupted(
              DecodingError.Context(
                codingPath: decoder.codingPath,
                debugDescription: "Multiple values set for oneof 'failure'"))
          }
          failure = $0
        }
        if let quotaFailure = try container.decodeIfPresent(
          TargetSite.FailureReason.QuotaFailure?.self, forKey: .quotaFailure)
        {
          try failureCheckAndSet(.quotaFailure(quotaFailure))
        }
        self.failure = failure
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        if let choice = self.failure {
          switch choice {
          case .quotaFailure(let value):
            try container.encode(value, forKey: .quotaFailure)
          }
        }
      }

      /// Failed due to insufficient quota.
      public struct QuotaFailure: Codable, Equatable, GoogleCloudWkt._AnyPackable,
        Sendable
      {
        /// This number is an estimation on how much total quota this project needs
        /// to successfully complete indexing.
        public var totalRequiredQuota: Swift.Int64 = Swift.Int64()

        /// Initialize a new instance of `QuotaFailure`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = QuotaFailure().with { $0.totalRequiredQuota = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        public static var _anyTypeUrl: Swift.String {
          return
            "type.googleapis.com/google.cloud.discoveryengine.v1.TargetSite.FailureReason.QuotaFailure"
        }
        public init(fromAny any: GoogleCloudWkt.`Any`) throws {
          self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWkt.Struct {
          return try GoogleCloudWkt._slowAnySerialize(message: self)
        }
      }

      /// Failure reason.
      public enum OneOf_Failure: Codable, Equatable, Sendable {
        /// Failed due to insufficient quota.
        indirect case quotaFailure(TargetSite.FailureReason.QuotaFailure?)
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.cloud.discoveryengine.v1.TargetSite.FailureReason"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    /// Possible target site types.
    public enum Type_: Codable, Equatable, Sendable {
      /// This value is unused. In this case, server behavior defaults to
      /// [Type.INCLUDE][google.cloud.discoveryengine.v1.TargetSite.Type.INCLUDE].
      ///
      /// [google.cloud.discoveryengine.v1.TargetSite.Type.INCLUDE]: <doc:TargetSite/Type_/include>
      case unspecified
      /// Include the target site.
      case include
      /// Exclude the target site.
      case exclude
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .include: return 1
        case .exclude: return 2
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "TYPE_UNSPECIFIED"
        case .include: return "INCLUDE"
        case .exclude: return "EXCLUDE"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:Type_/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "TYPE_UNSPECIFIED": self = .unspecified
        case "INCLUDE": self = .include
        case "EXCLUDE": self = .exclude
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:Type_/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .include
        case 2: self = .exclude
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .include: return try container.encode(1)
        case .exclude: return try container.encode(2)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    /// Target site indexing status enumeration.
    public enum IndexingStatus: Codable, Equatable, Sendable {
      /// Defaults to SUCCEEDED.
      case unspecified
      /// The target site is in the update queue and will be picked up by indexing
      /// pipeline.
      case pending
      /// The target site fails to be indexed.
      case failed
      /// The target site has been indexed.
      case succeeded
      /// The previously indexed target site has been marked to be deleted. This is
      /// a transitioning state which will resulted in either:
      /// 1. target site deleted if unindexing is successful;
      /// 2. state reverts to SUCCEEDED if the unindexing fails.
      case deleting
      /// The target site change is pending but cancellable.
      case cancellable
      /// The target site change is cancelled.
      case cancelled
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .pending: return 1
        case .failed: return 2
        case .succeeded: return 3
        case .deleting: return 4
        case .cancellable: return 5
        case .cancelled: return 6
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "INDEXING_STATUS_UNSPECIFIED"
        case .pending: return "PENDING"
        case .failed: return "FAILED"
        case .succeeded: return "SUCCEEDED"
        case .deleting: return "DELETING"
        case .cancellable: return "CANCELLABLE"
        case .cancelled: return "CANCELLED"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:IndexingStatus/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "INDEXING_STATUS_UNSPECIFIED": self = .unspecified
        case "PENDING": self = .pending
        case "FAILED": self = .failed
        case "SUCCEEDED": self = .succeeded
        case "DELETING": self = .deleting
        case "CANCELLABLE": self = .cancellable
        case "CANCELLED": self = .cancelled
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:IndexingStatus/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .pending
        case 2: self = .failed
        case 3: self = .succeeded
        case 4: self = .deleting
        case 5: self = .cancellable
        case 6: self = .cancelled
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .pending: return try container.encode(1)
        case .failed: return try container.encode(2)
        case .succeeded: return try container.encode(3)
        case .deleting: return try container.encode(4)
        case .cancellable: return try container.encode(5)
        case .cancelled: return try container.encode(6)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.discoveryengine.v1.TargetSite"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }
#endif
