// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

#if DataStoreService
  import Foundation
  import GoogleCloudWkt

  /// Config to store data store type configuration for workspace data
  public struct WorkspaceConfig: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// The Google Workspace data source.
    public var type: WorkspaceConfig.Type_ = WorkspaceConfig.Type_()

    /// Obfuscated Dasher customer ID.
    public var dasherCustomerId: Swift.String = Swift.String()

    /// Optional. The super admin service account for the workspace that will be
    /// used for access token generation. For now we only use it for Native Google
    /// Drive connector data ingestion.
    public var superAdminServiceAccount: Swift.String = Swift.String()

    /// Optional. The super admin email address for the workspace that will be used
    /// for access token generation. For now we only use it for Native Google Drive
    /// connector data ingestion.
    public var superAdminEmailAddress: Swift.String = Swift.String()

    /// Initialize a new instance of `WorkspaceConfig`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = WorkspaceConfig().with { $0.type = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// Specifies the type of Workspace App supported by this DataStore
    public enum Type_: Codable, Equatable, Sendable {
      /// Defaults to an unspecified Workspace type.
      case unspecified
      /// Workspace Data Store contains Drive data
      case googleDrive
      /// Workspace Data Store contains Mail data
      case googleMail
      /// Workspace Data Store contains Sites data
      case googleSites
      /// Workspace Data Store contains Calendar data
      case googleCalendar
      /// Workspace Data Store contains Chat data
      case googleChat
      /// Workspace Data Store contains Groups data
      case googleGroups
      /// Workspace Data Store contains Keep data
      case googleKeep
      /// Workspace Data Store contains People data
      case googlePeople
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .googleDrive: return 1
        case .googleMail: return 2
        case .googleSites: return 3
        case .googleCalendar: return 4
        case .googleChat: return 5
        case .googleGroups: return 6
        case .googleKeep: return 7
        case .googlePeople: return 8
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "TYPE_UNSPECIFIED"
        case .googleDrive: return "GOOGLE_DRIVE"
        case .googleMail: return "GOOGLE_MAIL"
        case .googleSites: return "GOOGLE_SITES"
        case .googleCalendar: return "GOOGLE_CALENDAR"
        case .googleChat: return "GOOGLE_CHAT"
        case .googleGroups: return "GOOGLE_GROUPS"
        case .googleKeep: return "GOOGLE_KEEP"
        case .googlePeople: return "GOOGLE_PEOPLE"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to ``.unknownStringValue(_:)``.
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "TYPE_UNSPECIFIED": self = .unspecified
        case "GOOGLE_DRIVE": self = .googleDrive
        case "GOOGLE_MAIL": self = .googleMail
        case "GOOGLE_SITES": self = .googleSites
        case "GOOGLE_CALENDAR": self = .googleCalendar
        case "GOOGLE_CHAT": self = .googleChat
        case "GOOGLE_GROUPS": self = .googleGroups
        case "GOOGLE_KEEP": self = .googleKeep
        case "GOOGLE_PEOPLE": self = .googlePeople
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to ``.unknownIntValue(_:)``.
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .googleDrive
        case 2: self = .googleMail
        case 3: self = .googleSites
        case 4: self = .googleCalendar
        case 5: self = .googleChat
        case 6: self = .googleGroups
        case 7: self = .googleKeep
        case 8: self = .googlePeople
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .googleDrive: return try container.encode(1)
        case .googleMail: return try container.encode(2)
        case .googleSites: return try container.encode(3)
        case .googleCalendar: return try container.encode(4)
        case .googleChat: return try container.encode(5)
        case .googleGroups: return try container.encode(6)
        case .googleKeep: return try container.encode(7)
        case .googlePeople: return try container.encode(8)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.discoveryengine.v1.WorkspaceConfig"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }
#endif
