// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

#if CmekConfigService || DataStoreService || IdentityMappingStoreService
  import Foundation
  import GoogleCloudWkt

  /// Configurations used to enable CMEK data encryption with Cloud KMS keys.
  public struct CmekConfig: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Required. The name of the CmekConfig of the form
    /// `projects/{project}/locations/{location}/cmekConfig` or
    /// `projects/{project}/locations/{location}/cmekConfigs/{cmek_config}`.
    public var name: Swift.String = Swift.String()

    /// KMS key resource name which will be used to encrypt resources
    /// `projects/{project}/locations/{location}/keyRings/{keyRing}/cryptoKeys/{keyId}`.
    public var kmsKey: Swift.String = Swift.String()

    /// KMS key version resource name which will be used to encrypt resources
    /// `<kms_key>/cryptoKeyVersions/{keyVersion}`.
    public var kmsKeyVersion: Swift.String = Swift.String()

    /// Output only. The states of the CmekConfig.
    public var state: CmekConfig.State = CmekConfig.State()

    /// Output only. The default CmekConfig for the Customer.
    public var isDefault: Swift.Bool = Swift.Bool()

    /// Output only. The timestamp of the last key rotation.
    public var lastRotationTimestampMicros: Swift.Int64 = Swift.Int64()

    /// Optional. Single-regional CMEKs that are required for some VAIS features.
    public var singleRegionKeys: [SingleRegionKey] = []

    /// Output only. Whether the NotebookLM Corpus is ready to be used.
    public var notebooklmState: CmekConfig.NotebookLMState = CmekConfig.NotebookLMState()

    /// Initialize a new instance of `CmekConfig`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = CmekConfig().with { $0.name = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// States of the CmekConfig.
    public enum State: Codable, Equatable, Sendable {
      /// The CmekConfig state is unknown.
      case unspecified
      /// The CmekConfig is creating.
      case creating
      /// The CmekConfig can be used with DataStores.
      case active
      /// The CmekConfig is unavailable, most likely due to the KMS Key being
      /// revoked.
      case keyIssue
      /// The CmekConfig is deleting.
      case deleting
      /// The CmekConfig is not usable, most likely due to some internal issue.
      case unusable
      /// The KMS key version is being rotated.
      case activeRotating
      /// The CmekConfig deletion process failed.
      case deleteFailed
      /// The KMS key is soft deleted. Some cleanup policy will eventually be
      /// applied.
      case deleted
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .creating: return 1
        case .active: return 2
        case .keyIssue: return 3
        case .deleting: return 4
        case .unusable: return 5
        case .activeRotating: return 6
        case .deleteFailed: return 7
        case .deleted: return 8
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "STATE_UNSPECIFIED"
        case .creating: return "CREATING"
        case .active: return "ACTIVE"
        case .keyIssue: return "KEY_ISSUE"
        case .deleting: return "DELETING"
        case .unusable: return "UNUSABLE"
        case .activeRotating: return "ACTIVE_ROTATING"
        case .deleteFailed: return "DELETE_FAILED"
        case .deleted: return "DELETED"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:State/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "STATE_UNSPECIFIED": self = .unspecified
        case "CREATING": self = .creating
        case "ACTIVE": self = .active
        case "KEY_ISSUE": self = .keyIssue
        case "DELETING": self = .deleting
        case "DELETE_FAILED": self = .deleteFailed
        case "UNUSABLE": self = .unusable
        case "ACTIVE_ROTATING": self = .activeRotating
        case "DELETED": self = .deleted
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:State/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .creating
        case 2: self = .active
        case 3: self = .keyIssue
        case 4: self = .deleting
        case 5: self = .unusable
        case 6: self = .activeRotating
        case 7: self = .deleteFailed
        case 8: self = .deleted
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .creating: return try container.encode(1)
        case .active: return try container.encode(2)
        case .keyIssue: return try container.encode(3)
        case .deleting: return try container.encode(4)
        case .unusable: return try container.encode(5)
        case .activeRotating: return try container.encode(6)
        case .deleteFailed: return try container.encode(7)
        case .deleted: return try container.encode(8)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    /// States of NotebookLM.
    public enum NotebookLMState: Codable, Equatable, Sendable {
      /// The NotebookLM state is unknown.
      case unspecified
      /// The NotebookLM is not ready.
      case notebookLmNotReady
      /// The NotebookLM is ready to be used.
      case notebookLmReady
      /// The NotebookLM is not enabled.
      case notebookLmNotEnabled
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .notebookLmNotReady: return 1
        case .notebookLmReady: return 2
        case .notebookLmNotEnabled: return 3
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "NOTEBOOK_LM_STATE_UNSPECIFIED"
        case .notebookLmNotReady: return "NOTEBOOK_LM_NOT_READY"
        case .notebookLmReady: return "NOTEBOOK_LM_READY"
        case .notebookLmNotEnabled: return "NOTEBOOK_LM_NOT_ENABLED"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:NotebookLMState/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "NOTEBOOK_LM_STATE_UNSPECIFIED": self = .unspecified
        case "NOTEBOOK_LM_NOT_READY": self = .notebookLmNotReady
        case "NOTEBOOK_LM_READY": self = .notebookLmReady
        case "NOTEBOOK_LM_NOT_ENABLED": self = .notebookLmNotEnabled
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:NotebookLMState/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .notebookLmNotReady
        case 2: self = .notebookLmReady
        case 3: self = .notebookLmNotEnabled
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .notebookLmNotReady: return try container.encode(1)
        case .notebookLmReady: return try container.encode(2)
        case .notebookLmNotEnabled: return try container.encode(3)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.discoveryengine.v1.CmekConfig"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }
#endif
