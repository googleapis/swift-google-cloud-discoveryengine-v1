// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

#if AssistantService || ConversationalSearchService || SessionService
  import Foundation
  import GoogleCloudWkt

  /// AssistAnswer resource, main part of
  /// [AssistResponse][google.cloud.discoveryengine.v1.AssistResponse].
  public struct AssistAnswer: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Immutable. Resource name of the `AssistAnswer`.
    /// Format:
    /// `projects/{project}/locations/{location}/collections/{collection}/engines/{engine}/sessions/{session}/assistAnswers/{assist_answer}`
    ///
    /// This field must be a UTF-8 encoded string with a length limit of 1024
    /// characters.
    public var name: Swift.String = Swift.String()

    /// State of the answer generation.
    public var state: AssistAnswer.State = AssistAnswer.State()

    /// Replies of the assistant.
    public var replies: [AssistAnswer.Reply] = []

    /// Reasons for not answering the assist call.
    public var assistSkippedReasons: [AssistAnswer.AssistSkippedReason] = []

    /// Initialize a new instance of `AssistAnswer`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = AssistAnswer().with { $0.name = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// One part of the multi-part response of the assist call.
    public struct Reply: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// Alternatives for the assistant reply.
      public var reply: OneOf_Reply? = nil

      /// Initialize a new instance of `Reply`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = Reply().with { $0.groundedContent = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      private enum CodingKeys: Swift.String, CodingKey {
        case groundedContent = "groundedContent"
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        var reply: OneOf_Reply? = nil
        let replyCheckAndSet = {
          if reply != nil {
            throw DecodingError.dataCorrupted(
              DecodingError.Context(
                codingPath: decoder.codingPath,
                debugDescription: "Multiple values set for oneof 'reply'"))
          }
          reply = $0
        }
        if let groundedContent = try container.decodeIfPresent(
          AssistantGroundedContent?.self, forKey: .groundedContent)
        {
          try replyCheckAndSet(.groundedContent(groundedContent))
        }
        self.reply = reply
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        if let choice = self.reply {
          switch choice {
          case .groundedContent(let value):
            try container.encode(value, forKey: .groundedContent)
          }
        }
      }

      /// Alternatives for the assistant reply.
      public enum OneOf_Reply: Codable, Equatable, Sendable {
        /// Possibly grounded response text or media from the assistant.
        indirect case groundedContent(AssistantGroundedContent?)
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.cloud.discoveryengine.v1.AssistAnswer.Reply"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    /// State of the answer generation.
    public enum State: Codable, Equatable, Sendable {
      /// Unknown.
      case unspecified
      /// Assist operation is currently in progress.
      case inProgress
      /// Assist operation has failed.
      case failed
      /// Assist operation has succeeded.
      case succeeded
      /// Assist operation has been skipped.
      case skipped
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .inProgress: return 1
        case .failed: return 2
        case .succeeded: return 3
        case .skipped: return 4
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "STATE_UNSPECIFIED"
        case .inProgress: return "IN_PROGRESS"
        case .failed: return "FAILED"
        case .succeeded: return "SUCCEEDED"
        case .skipped: return "SKIPPED"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:State/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "STATE_UNSPECIFIED": self = .unspecified
        case "IN_PROGRESS": self = .inProgress
        case "FAILED": self = .failed
        case "SUCCEEDED": self = .succeeded
        case "SKIPPED": self = .skipped
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:State/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .inProgress
        case 2: self = .failed
        case 3: self = .succeeded
        case 4: self = .skipped
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .inProgress: return try container.encode(1)
        case .failed: return try container.encode(2)
        case .succeeded: return try container.encode(3)
        case .skipped: return try container.encode(4)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    /// Possible reasons for not answering an assist call.
    public enum AssistSkippedReason: Codable, Equatable, Sendable {
      /// Default value. Skip reason is not specified.
      case unspecified
      /// The assistant ignored the query, because it did not appear to be
      /// answer-seeking.
      case nonAssistSeekingQueryIgnored
      /// The assistant ignored the query or refused to answer because of a
      /// customer policy violation (e.g., the query or the answer contained a
      /// banned phrase).
      case customerPolicyViolation
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .nonAssistSeekingQueryIgnored: return 1
        case .customerPolicyViolation: return 2
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "ASSIST_SKIPPED_REASON_UNSPECIFIED"
        case .nonAssistSeekingQueryIgnored: return "NON_ASSIST_SEEKING_QUERY_IGNORED"
        case .customerPolicyViolation: return "CUSTOMER_POLICY_VIOLATION"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:AssistSkippedReason/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "ASSIST_SKIPPED_REASON_UNSPECIFIED": self = .unspecified
        case "NON_ASSIST_SEEKING_QUERY_IGNORED": self = .nonAssistSeekingQueryIgnored
        case "CUSTOMER_POLICY_VIOLATION": self = .customerPolicyViolation
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:AssistSkippedReason/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .nonAssistSeekingQueryIgnored
        case 2: self = .customerPolicyViolation
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .nonAssistSeekingQueryIgnored: return try container.encode(1)
        case .customerPolicyViolation: return try container.encode(2)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.discoveryengine.v1.AssistAnswer"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }
#endif
