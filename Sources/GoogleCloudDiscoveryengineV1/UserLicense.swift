// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

#if UserLicenseService
  import Foundation
  import GoogleCloudWkt

  /// User License information assigned by the admin.
  public struct UserLicense: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Required. Immutable. The user principal of the User, could be email address
    /// or other prinical identifier. This field is immutable. Admin assign
    /// licenses based on the user principal.
    public var userPrincipal: Swift.String = Swift.String()

    /// Optional. The user profile.
    /// We user user full name(First name + Last name) as user profile.
    public var userProfile: Swift.String = Swift.String()

    /// Output only. License assignment state of the user.
    /// If the user is assigned with a license config, the user loggin will be
    /// assigned with the license;
    /// If the user's license assignment state is unassigned or unspecified, no
    /// license config will be associated to the user;
    public var licenseAssignmentState: UserLicense.LicenseAssignmentState =
      UserLicense.LicenseAssignmentState()

    /// Optional. The full resource name of the Subscription(LicenseConfig)
    /// assigned to the user.
    public var licenseConfig: Swift.String = Swift.String()

    /// Output only. User created timestamp.
    public var createTime: GoogleCloudWkt.Timestamp? = nil

    /// Output only. User update timestamp.
    public var updateTime: GoogleCloudWkt.Timestamp? = nil

    /// Output only. User last logged in time.
    /// If the user has not logged in yet, this field will be empty.
    public var lastLoginTime: GoogleCloudWkt.Timestamp? = nil

    /// Initialize a new instance of `UserLicense`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = UserLicense().with { $0.userPrincipal = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// License assignment state enumeration.
    public enum LicenseAssignmentState: Codable, Equatable, Sendable {
      /// Default value.
      case unspecified
      /// License assigned to the user.
      case assigned
      /// No license assigned to the user.
      /// Deprecated, translated to NO_LICENSE.
      case unassigned
      /// No license assigned to the user.
      case noLicense
      /// User attempted to login but no license assigned to the user.
      /// This state is only used for no user first time login attempt but cannot
      /// get license assigned.
      /// Users already logged in but cannot get license assigned will be assigned
      /// NO_LICENSE state(License could be unassigned by admin).
      case noLicenseAttemptedLogin
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .assigned: return 1
        case .unassigned: return 2
        case .noLicense: return 3
        case .noLicenseAttemptedLogin: return 4
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "LICENSE_ASSIGNMENT_STATE_UNSPECIFIED"
        case .assigned: return "ASSIGNED"
        case .unassigned: return "UNASSIGNED"
        case .noLicense: return "NO_LICENSE"
        case .noLicenseAttemptedLogin: return "NO_LICENSE_ATTEMPTED_LOGIN"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to ``.unknownStringValue(_:)``.
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "LICENSE_ASSIGNMENT_STATE_UNSPECIFIED": self = .unspecified
        case "ASSIGNED": self = .assigned
        case "UNASSIGNED": self = .unassigned
        case "NO_LICENSE": self = .noLicense
        case "NO_LICENSE_ATTEMPTED_LOGIN": self = .noLicenseAttemptedLogin
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to ``.unknownIntValue(_:)``.
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .assigned
        case 2: self = .unassigned
        case 3: self = .noLicense
        case 4: self = .noLicenseAttemptedLogin
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .assigned: return try container.encode(1)
        case .unassigned: return try container.encode(2)
        case .noLicense: return try container.encode(3)
        case .noLicenseAttemptedLogin: return try container.encode(4)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.discoveryengine.v1.UserLicense"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }
#endif
